# hp28008 显示屏 LVGL 效果 PC 模拟 Implementation Plan

> **状态：已实现（Task 1-5 完成，Task 6 / 附录 A 暂不执行）。**

> **For agentic workers:** 本实现采用 superpowers:subagent-driven-development——逐个 Task 派发独立 implementer subagent 实现（controller 提供该 Task 完整文本与上下文，不让 subagent 读本文件），每个 Task 完成后做两阶段 review（spec 合规由 controller 自查或 `generalPurpose` subagent；代码质量由 `code-reviewer` subagent），通过后再在 TodoWrite 标记完成并进入下一个 Task。Task tool 不指定 model 参数以让 subagent 继承父模型（本仓库要求父子模型一致）。步骤用 `- [ ]` / `- [x]` 跟踪，执行后回填“执行结果”。

**Goal:** 把真机 UI 从 `hp28008.c` 解耦为可移植 UI 层（`main/ui_app/`，硬件数据经回调注入；并记录 Observer/Subject 演进法），使同一份 UI 免烧录即可在 PC 查看/交互。默认接入后端 A（纯 LVGL SDL，支持鼠标触摸，窗口 240x320）。

**范围与后端选择：** 本 PR **着重「解耦（Task 1-3）+ 后端 A（Task 4-5，纯 LVGL SDL，支持触摸）」**。**Task 6（后端 B / QEMU 复用）与附录 A（Observer/Subject 演进）本 PR 暂不执行**，留作后续单独 PR。C（LovyanGFX SDL）见 spec 第 10 节，本 plan 不含。

**Architecture:** 解耦阶段新增 `main/ui_app/{ui_app.h,ui_app.c}`（仅依赖 LVGL），`hp28008.c` 把 UI 构建委托给 `ui_app_create()` 并注入 `real_clock_source`（读 BM8563 RTC）；PC 后端在仓库内新增 `pc_simulator/`（CMake + SDL2 + LVGL FetchContent v9.5.0）复用 `main/ui_app/`。分辨率由 `ui_app.h` 的可移植宏 `UI_APP_HOR_RES/UI_APP_VER_RES` 提供（与真机 `hp28008.h` 的 `EXAMPLE_LCD_H_RES/V_RES` 同值、注释交叉标注）。设计依据见 [docs/superpowers/specs/2026-07-03-hp28008-lvgl-pc-sim-design.md](https://github.com/yuangezhizao/ESP-Pocket2/blob/dev/docs/superpowers/specs/2026-07-03-hp28008-lvgl-pc-sim-design.md)。

**Tech Stack:** ESP-IDF v5.5.4 (esp32s3)、LVGL v9.5.0、SDL2、CMake。

**参考：**

- LVGL SDL 驱动：[https://lvgl.io/docs/open/9.5/integration/pc/sdl.html](https://lvgl.io/docs/open/9.5/integration/pc/sdl.html)
- LVGL PC 模拟工程：[https://github.com/lvgl/lv_port_pc_vscode](https://github.com/lvgl/lv_port_pc_vscode)
- LVGL v9 Observer：[https://lvgl.io/docs/open/9.5/main-modules/observer/observer.html](https://lvgl.io/docs/open/9.5/main-modules/observer/observer.html)
- 设计文档：[docs/superpowers/specs/2026-07-03-hp28008-lvgl-pc-sim-design.md](https://github.com/yuangezhizao/ESP-Pocket2/blob/dev/docs/superpowers/specs/2026-07-03-hp28008-lvgl-pc-sim-design.md)

---

## Task 1: 新建可移植 UI 层 main/ui_app（回调注入）

**Files:**

- Create: `main/ui_app/ui_app.h`
- Create: `main/ui_app/ui_app.c`

- [x] **Step 1:** 创建 `main/ui_app/ui_app.h`（仅依赖 lvgl.h；定义可移植分辨率宏 + 回调注入接口）。

```c
/*
 * SPDX-FileCopyrightText: 2026 ESP-Pocket2
 *
 * SPDX-License-Identifier: Apache-2.0
 *
 * 可移植 UI 层：仅依赖 LVGL，不含任何硬件/ESP-IDF 依赖，以便同一份界面代码在真机、QEMU、PC(SDL) 等后端复用。硬件数据源(如 RTC 时间)通过回调注入，实现 UI 与硬件解耦。
 */
#pragma once

#include "lvgl.h"

#ifdef __cplusplus
extern "C"
{
#endif

/* hp28008 目标画布尺寸（可移植、无硬件依赖），供 PC/QEMU 等非 ESP 后端创建显示/窗口用。
 * 数值须与真机 hp28008.h 的 EXAMPLE_LCD_H_RES/V_RES 保持一致（均为 hp28008 物理 240x320）。
 * 因 hp28008.h 含大量 ESP-IDF 头依赖、PC 无法 include，故此处另置同值宏（两处定义、约定同值，非同一处单一来源）。 */
#define UI_APP_HOR_RES 240
#define UI_APP_VER_RES 320

    /* 时钟文本数据源：返回当前要显示的时间字符串；UI 不关心其来源(真机读 RTC / PC 读系统时间等)。 */
    typedef const char *(*ui_clock_source_fn)(void);

    /*
     * 在指定 screen 上创建可移植 UI（logo + 欢迎语 + 时钟 + 旋转按钮）。
     * 仅应调用一次（一次性构建，不支持重复调用：会重复创建 timer 并悬挂旧对象）。
     * scr: 目标屏幕对象(通常为 lv_screen_active())。
     * clock_src: 时钟文本提供者；传 NULL 时时钟显示占位串。
     */
    void ui_app_create(lv_obj_t *scr, ui_clock_source_fn clock_src);

#ifdef __cplusplus
}
#endif
```

- [x] **Step 2:** 创建 `main/ui_app/ui_app.c`（迁入 `app_main_display()` 的纯 UI；时钟走注入回调；宽度用 `lv_pct` 而非分辨率宏；旋转按钮从被点击 widget 所属 display 取旋转对象，兼容多 display）。

```c
/*
 * SPDX-FileCopyrightText: 2022-2024 Espressif Systems (Shanghai) CO LTD
 * SPDX-FileContributor: 2026 ESP-Pocket2
 *
 * SPDX-License-Identifier: Apache-2.0
 *
 * 可移植 UI 层实现：由 main/lvgl/hp28008.c 的 app_main_display() 抽离而来，去除对 RTC 等硬件的直接依赖，时钟数据源改为回调注入。仅依赖 LVGL，可在真机/QEMU/PC(SDL) 复用。
 */
#include "ui_app.h"

/* LVGL image declare（图片为可移植 C array，真机由构建系统生成、PC 由 pc_simulator/CMakeLists.txt 构建时动态生成到 build 目录） */
LV_IMG_DECLARE(esp_logo)

static lv_obj_t *s_clock_label = NULL;
static ui_clock_source_fn s_clock_src = NULL;
static lv_timer_t *s_clock_timer = NULL;

static void ui_app_rotate_btn_cb(lv_event_t *e)
{
    /* 从被点击按钮所属 display 取旋转对象，兼容多 display（不依赖 default display） */
    lv_obj_t *btn = lv_event_get_target_obj(e);
    lv_display_t *disp = lv_obj_get_display(btn);
    lv_disp_rotation_t rotation = lv_disp_get_rotation(disp);
    rotation++;
    if (rotation > LV_DISPLAY_ROTATION_270)
    {
        rotation = LV_DISPLAY_ROTATION_0;
    }
    lv_disp_set_rotation(disp, rotation);
}

static void ui_app_clock_timer_cb(lv_timer_t *timer)
{
    LV_UNUSED(timer);
    /* s_clock_label 由 ui_app_create 一次性设置，之后不会归零。
     * 单画面嵌入式用途下屏幕不会被销毁，故当前安全。 */
    const char *txt = (s_clock_src != NULL) ? s_clock_src() : NULL;
    lv_label_set_text(s_clock_label, (txt != NULL) ? txt : "Loading time...");
}

void ui_app_create(lv_obj_t *scr, ui_clock_source_fn clock_src)
{
    LV_ASSERT_MSG(s_clock_label == NULL, "ui_app_create: must only be called once");
    s_clock_src = clock_src;

    /* Create image */
    lv_obj_t *img_logo = lv_img_create(scr);
    lv_img_set_src(img_logo, &esp_logo);
    lv_obj_align(img_logo, LV_ALIGN_TOP_MID, 0, 20);

    /* Label（宽度取满屏配合居中；用 lv_pct 保持可移植） */
    lv_obj_t *label = lv_label_create(scr);
    lv_obj_set_width(label, lv_pct(100));
    lv_obj_set_style_text_align(label, LV_TEXT_ALIGN_CENTER, 0);
#if LVGL_VERSION_MAJOR == 8
    lv_label_set_recolor(label, true);
    lv_label_set_text(label, "#FF0000 " LV_SYMBOL_BELL " Hello world Espressif and LVGL " LV_SYMBOL_BELL "#\n#FF9400 " LV_SYMBOL_WARNING " For simplier initialization, use BSP " LV_SYMBOL_WARNING " #");
#else
    lv_label_set_text(label, LV_SYMBOL_BELL " Hello world Espressif and LVGL " LV_SYMBOL_BELL "\n " LV_SYMBOL_WARNING " For simplier initialization, use BSP " LV_SYMBOL_WARNING);
#endif
    lv_obj_align(label, LV_ALIGN_CENTER, 0, 20);

    /* Clock Label */
    s_clock_label = lv_label_create(scr);
    lv_obj_set_width(s_clock_label, lv_pct(100));
    lv_obj_set_style_text_align(s_clock_label, LV_TEXT_ALIGN_CENTER, 0);
    lv_label_set_text(s_clock_label, "Loading time...");
    lv_obj_align(s_clock_label, LV_ALIGN_CENTER, 0, 80);

    /* Start LVGL clock timer (for UI updates only) */
    s_clock_timer = lv_timer_create(ui_app_clock_timer_cb, 1000, NULL);

    /* Button */
    lv_obj_t *btn = lv_btn_create(scr);
    label = lv_label_create(btn);
    lv_label_set_text_static(label, "Rotate screen");
    lv_obj_align(btn, LV_ALIGN_BOTTOM_MID, 0, -30);
    lv_obj_add_event_cb(btn, ui_app_rotate_btn_cb, LV_EVENT_CLICKED, NULL);
}
```

- [x] **Step 3:** 提交。

```bash
git add main/ui_app/ui_app.h main/ui_app/ui_app.c
git commit -m "$(cat <<'EOF'
✨ Feat(main/ui_app): 新增可移植 UI 层(回调注入解耦硬件)

- ui_app_create() 迁入原 app_main_display 的纯 LVGL 界面(logo/欢迎语/时钟/旋转按钮)，约定一次性调用
- 时钟数据源改 ui_clock_source_fn 回调注入，UI 不再直接依赖 BM8563 RTC
- 新增可移植分辨率宏 UI_APP_HOR_RES/UI_APP_VER_RES(240/320)，与 hp28008.h 同值(注释交叉标注)
- s_clock_timer 保存 lv_timer_create 返回值，便于将来 timer 生命周期管理
- 宽度用 lv_pct、旋转从按钮所属 display 取(兼容多 display)，去除硬件依赖，可跨真机/QEMU/PC 复用
EOF
)"
```

---

## Task 2: 真机侧接入 ui_app + CMake 注册

**Files:**

- Modify: `main/lvgl/hp28008.c`
- Modify: `main/CMakeLists.txt`

- [x] **Step 1:** `hp28008.c` 顶部 `#include "hp28008.h"` 后新增 `#include "ui_app.h"`；移除本文件的 `LV_IMG_DECLARE(esp_logo)`（迁至 ui_app.c）与静态变量 `clock_label` / `clock_timer`。
- [x] **Step 2:** `hp28008.c` 用下述内容替换 `_app_button_cb` + `_clock_timer_cb` + `app_main_display` 三个函数（即新增 `real_clock_source` 适配 BM8563，`app_main_display` 委托 `ui_app_create`；旋转逻辑随 UI 迁至 ui_app.c 故此处删除按钮回调）。

```c
/* 真机时钟数据源：读取 BM8563 RTC 并返回格式化字符串，注入给可移植 UI 层 */
static const char *real_clock_source(void)
{
    update_RTC_global_variable();
    return RTC_global_variable;
}

void app_main_display(void)
{
    ESP_LOGI(TAG, "Display LVGL example");

#if EXAMPLE_LCD_DIRECTION == (90)
    lv_disp_set_rotation(lvgl_disp, LV_DISPLAY_ROTATION_90);
#elif EXAMPLE_LCD_DIRECTION == (180)
    lv_disp_set_rotation(lvgl_disp, LV_DISPLAY_ROTATION_180);
#elif EXAMPLE_LCD_DIRECTION == (270)
    lv_disp_set_rotation(lvgl_disp, LV_DISPLAY_ROTATION_270);
#endif

    /* Task lock（LVGL API 非线程安全） */
    lvgl_port_lock(0);

    lv_obj_t *scr = lv_scr_act();

    /* 可移植 UI 层，硬件时钟数据源通过回调注入 */
    ui_app_create(scr, real_clock_source);

    /* Task unlock */
    lvgl_port_unlock();
}
```

- [x] **Step 3:** `main/CMakeLists.txt` 注册 ui_app：新增 glob、加入 SRCS 与 INCLUDE_DIRS。

```cmake
# 在其它 GLOB 之后追加
file(GLOB_RECURSE UI_APP_C_SOURCES ${CMAKE_SOURCE_DIR} "ui_app/*.c")
# SRCS 里 ${LVGL_QEMU_RGB_C_SOURCES} 之后追加
    ${UI_APP_C_SOURCES}
# INCLUDE_DIRS 里 "lvgl_qemu_rgb" 之后追加
    "ui_app"
```

- [x] **Step 4:** 提交。

```bash
git add main/lvgl/hp28008.c main/CMakeLists.txt
git commit -m "$(cat <<'EOF'
♻️ Refactor(main/lvgl/hp28008.c): 真机 UI 委托 ui_app + 注入 RTC 数据源

- app_main_display() 改为调用 ui_app_create(scr, real_clock_source)，UI 构建下沉到可移植层
- 新增 real_clock_source() 适配 BM8563 RTC(update_RTC_global_variable/RTC_global_variable)
- 移除已迁出的 clock_label/clock_timer/_clock_timer_cb/_app_button_cb 与 esp_logo 声明
- main/CMakeLists.txt 注册 ui_app 源码与 include 目录
EOF
)"
```

---

## Task 3: 解耦阶段构建验证（真机路径不破）

**Files:** 无（仅构建）

- [x] **Step 1:** `source /opt/esp/idf/export.sh`。
- [x] **Step 2:**（必做）当前 demo 配置 `idf.py build`，确认 `hp28008.c` + `ui_app` 编译通过（无 v9 API 错误、无符号冲突、无 unused 报错）。注：当前启用的是 `main_lvgl_qemu_rgb.c`，但 `hp28008.c`/`ui_app` 属组件 GLOB 源码，仍会一并编译。
- [x] **Step 3:**（必做）验证真机入口链路：临时把 `main/CMakeLists.txt` 的 `SRCS` 从 `main_lvgl_qemu_rgb.c` 切到 `main_lvgl.c` 再 `idf.py build`，确认真机入口 `app_main` → `app_main_display` → `ui_app_create` 编译通过后切回。此切换不提交。

---

## Task 4: PC/SDL 模拟工程 pc_simulator（后端 A，支持鼠标触摸）

**Files:**

- Create: `pc_simulator/CMakeLists.txt`
- Create: `pc_simulator/main.c`
- Create: `pc_simulator/lv_conf.h`
- 复用: `main/ui_app/ui_app.c`、`main/ui_app/ui_app.h`
- `esp_logo.c` 由 CMakeLists 在构建时动态生成（不入库），`build` 忽略规则已在根 `.gitignore` 中
- 注：`pc_simulator/README.md` 在 Task 5 创建

- [x] **Step 1:** `pc_simulator/lv_conf.h`：复制 LVGL v9.5 的 `lv_conf_template.h`，把文件开头的 `#if 0` 改为 `#if 1` 使其生效；关键项明确设为（其余保持 template 默认）：
  - `LV_COLOR_DEPTH 16`（与真机 RGB565 一致）
  - `LV_USE_SDL 1`（启用 SDL 后端）
  - `LV_USE_OBSERVER 1`（默认即 1，显式确认，供附录 A 演进）
  - `LV_FONT_MONTSERRAT_14 1` 且 `LV_FONT_DEFAULT &lv_font_montserrat_14`（内建含 `LV_SYMBOL_BELL/WARNING` 所需 symbol）
  - `LV_USE_LOG 1`、`LV_LOG_LEVEL LV_LOG_LEVEL_WARN`（便于排查，可选）
  - `LV_MEM_SIZE` 保持 template 默认（如 `(64 * 1024U)`），不足再调
  - `LV_USE_DEMOS 0` / `LV_USE_EXAMPLES 0`（不需要）
- [x] **Step 2:** `pc_simulator/main.c`（窗口用 `UI_APP_HOR_RES/UI_APP_VER_RES` 宏；含 `SDL_MAIN_HANDLED`；时钟源带失败兜底）。

```c
/*
 * SPDX-FileCopyrightText: 2026 ESP-Pocket2
 *
 * SPDX-License-Identifier: Apache-2.0
 *
 * PC/SDL 模拟器入口：复用 main/ui_app 的可移植 UI 层，通过 lv_sdl_mouse 模拟触摸交互。
 */
#define SDL_MAIN_HANDLED   /* 修复 SDL 在部分平台的 WinMain/main 链接问题（LVGL 官方示例做法） */
#include "lvgl.h"
#include "ui_app.h"
#include <time.h>

/* PC 侧时钟数据源：用本机时间替代 BM8563 RTC；失败时返回占位串 */
static const char *pc_clock_source(void)
{
    static char buf[32];
    time_t now = time(NULL);
    struct tm tm_buf;
    struct tm *t = localtime_r(&now, &tm_buf);   /* localtime_r is thread-safe (POSIX) */
    if (t == NULL || strftime(buf, sizeof(buf), "%Y-%m-%d %H:%M:%S", t) == 0)
    {
        return "----/--/-- --:--:--";
    }
    return buf;
}

int main(void)
{
    lv_init();
    /* 窗口尺寸对齐 hp28008，取自 ui_app.h 的可移植宏，避免写死 240/320 */
    lv_display_t *disp = lv_sdl_window_create(UI_APP_HOR_RES, UI_APP_VER_RES);
    (void)disp;
    lv_indev_t *mouse = lv_sdl_mouse_create();   /* 鼠标模拟触摸 */
    (void)mouse;

    ui_app_create(lv_screen_active(), pc_clock_source);   /* 仅调用一次 */

    while (1)
    {
        lv_timer_handler();
        lv_delay_ms(5);
    }
    return 0;
}
```

- [x] **Step 3:** `esp_logo.c` 由 `CMakeLists.txt` 在构建时通过 `add_custom_command` 调用 LVGL 自带 `scripts/LVGLImage.py` 动态生成（ARGB8888，变量名 `esp_logo`），输出到 build 目录，不入库。Python 依赖（pypng lz4 Pillow）由 CMake 自动创建虚拟环境安装，无需手动 pip install。

- [x] **Step 4:** `pc_simulator/CMakeLists.txt`（LVGL FetchContent v9.5.0；`LV_BUILD_CONF_PATH` 指定 lv_conf.h；`if(POLICY CMP0169)` guard；`find_package(SDL2 CONFIG QUIET)` + module fallback；venv sentinel 幂等安装；`add_custom_command` 构建时生成 esp_logo.c）。见仓库最终 `pc_simulator/CMakeLists.txt`。

- [x] **Step 5:** `build` 忽略规则已加入根 `.gitignore`（不再为 pc_simulator 单独创建 `.gitignore`）。

- [x] **Step 6:** 构建运行：`sudo apt-get install -y libsdl2-dev cmake build-essential`；`rm -rf pc_simulator/build && cmake -S pc_simulator -B pc_simulator/build && cmake --build pc_simulator/build --parallel`；运行 `./pc_simulator/build/pc_simulator`，出现 240x320 窗口，显示 logo/欢迎语/时钟，鼠标点"Rotate screen"可旋转。云端无 X 时 `DISPLAY=:1` 或截图核对。
- [x] **Step 7:** 提交。

```bash
git add pc_simulator/
git commit -m "$(cat <<'EOF'
✨ Feat(pc_simulator): 新增 PC/SDL LVGL 模拟工程复用 hp28008 UI

- CMake + SDL2 + LVGL v9.5.0(FetchContent) 编译共享的 main/ui_app
- 窗口尺寸取 ui_app.h 的 UI_APP_HOR_RES/UI_APP_VER_RES 宏(240x320)，lv_sdl_mouse 鼠标模拟触摸
- pc_clock_source 用本机时间(带失败兜底)替代 BM8563 RTC；main.c 含 SDL_MAIN_HANDLED
- lv_conf.h：LV_COLOR_DEPTH=16，LV_USE_SDL=1，LV_USE_BUTTON，LV_LOG_PRINTF=1，
  LV_SDL_RENDER_MODE=PARTIAL（修复旋转；SDL sw backend 仅 PARTIAL 模式做像素旋转）
- esp_logo 由 LVGLImage.py 构建时从 main/images/esp_logo.png 生成（ARGB8888），不入库
  （cmake 配置自动创建 venv 并安装 pypng lz4 Pillow；对齐固件 lvgl_port_create_c_image 格式）
- CMakeLists：if(POLICY CMP0169) guard；find_package(SDL2 CONFIG QUIET) + module fallback；
  venv sentinel 幂等安装依赖；demos/examples 禁用加速构建；中文注释；SDL2 include 传给 lvgl（修复 macOS）
- .gitignore 新增 build（匹配任意层级构建目录）
EOF
)"
```

---

## Task 5: 文档与脚本

**Files:**

- Create: `pc_simulator/README.md`
- Modify: `AGENTS.md`

- [x] **Step 1:** 创建 `pc_simulator/README.md`，按下述章节骨架撰写（构建命令以 Task 4 定稿为准）：
  1. **简介**：本工程是复用 `main/ui_app/` 的纯 PC LVGL(SDL2) 模拟，用于免烧录预览 hp28008 的 LVGL UI 并支持鼠标模拟触摸；不经 ESP-IDF/QEMU。
  2. **依赖**：Linux `sudo apt-get install -y libsdl2-dev cmake build-essential`；macOS `brew install sdl2 cmake`；LVGL v9.5.0 由 CMake FetchContent 从 GitHub 自动拉取，无需手装。
  3. **构建与运行**：`rm -rf pc_simulator/build && cmake -S pc_simulator -B pc_simulator/build && cmake --build pc_simulator/build --parallel`（首次 configure 需联网：FetchContent 从 GitHub 拉 LVGL、并从 PyPI 装 pypng/lz4/Pillow；受限网络需配 Git/pip 代理或预缓存）；运行 `./pc_simulator/build/pc_simulator`。
  4. **交互**：鼠标左键 = 触摸；点“Rotate screen”旋转屏幕。
  5. **与固件的关系**：UI 源码共享 `main/ui_app/`，改 UI 两端同步；时钟数据源 PC 用本机时间（`pc_clock_source`）、真机用 BM8563（`real_clock_source`）。
  6. **分辨率**：窗口取 `ui_app.h` 的 `UI_APP_HOR_RES` / `UI_APP_VER_RES`(240x320)，须与真机 `hp28008.h` 的 `EXAMPLE_LCD_H_RES/V_RES` 同值。
  7. **esp_logo 资源**：`esp_logo.c` 由 CMake 在构建时自动生成（不入库）；Python 依赖（pypng lz4 Pillow）由 CMake 自动创建 venv 安装，无需手动操作；源图更新后重新 `rm -rf pc_simulator/build` 再构建即可。
  8. **平台说明**：主要在 Linux/macOS（含云端 `DISPLAY=:1`）验证；`main.c` 含 `SDL_MAIN_HANDLED`（SDL 官方示例惯例）；时钟用 POSIX `localtime_r`，暂未适配 Windows。
  9. **（可选）headless 截图**：`DISPLAY=:1` + `ffmpeg -f x11grab -i :1 -frames:v 1 -y shot.png`。
  10. **局限**：仅渲染 UI，不跑真实固件/驱动；需真实固件行为请用 QEMU 或真机。
- [x] **Step 2:** 修改 `AGENTS.md`：在“### 构建 / 运行 / 体积”小节**末尾（即“### 选择运行哪个 demo”小节之前）**追加如下小节（确切文字，不改既有 QEMU 段落结构）：

```markdown
### PC 模拟（纯 LVGL SDL，免烧录看 UI）

`pc_simulator/` 是复用 `main/ui_app/` 的纯 PC LVGL 模拟工程（SDL2，不经 ESP-IDF/QEMU），用于快速迭代 hp28008 的 LVGL 界面，并支持鼠标模拟触摸。构建运行见 `pc_simulator/README.md`。注意：它只渲染 UI、不跑真实固件/驱动；需要真实固件行为请用 QEMU 或真机。
```

- [x] **Step 3:** 提交。

```bash
git add pc_simulator/README.md AGENTS.md
git commit -m "$(cat <<'EOF'
📝 Docs(pc_simulator): 补 PC 模拟构建运行说明与 AGENTS 指引

- pc_simulator/README.md：简介/依赖/构建运行/交互/与固件关系/分辨率宏/esp_logo 来源/平台说明/截图/局限
- AGENTS.md 构建运行一节(选择 demo 之前)追加「PC 模拟（纯 LVGL SDL）」小节
EOF
)"
```

---

## Task 6（暂不执行 · 本 PR 聚焦解耦）: 后端 B —— QEMU 虚拟面板 240x320 复用同一 UI

> **本 PR 暂不执行**（着重「解耦 + 后端 A」）；此 Task 留作后续单独 PR 的记录。QEMU 无输入设备，按钮不可点属预期。以下步骤保持未勾，供将来执行参考。

**Files:**

- Create: `main/main_lvgl_qemu_uiapp.c`（新 demo 入口，避免动 PR3/5 既有散点图 demo）
- Modify: `main/lvgl_qemu_rgb/lvgl_qemu_rgb.*`（可选：参数化面板尺寸或复用现有 run 流程）
- Modify: `main/CMakeLists.txt`（SRCS 切到新入口）

- [ ] **Step 1:** 复用 `qemu_rgb_lvgl_run()` 的面板/缓冲/tick/task 初始化，但面板尺寸设 `UI_APP_HOR_RES x UI_APP_VER_RES`(240x320)，并把 `qemu_rgb_lvgl_demo_ui(disp)` 换成 `ui_app_create(lv_display_get_screen_active(disp), NULL)`（或注入一个 QEMU 侧假时钟源）。
- [ ] **Step 2:** `main/CMakeLists.txt` 的 `SRCS` 注释切到新入口，`idf.py build`。
- [ ] **Step 3:** `DISPLAY=:1 idf.py qemu --graphics --qemu-extra-args "-no-reboot"`，确认 240x320 窗口显示同一份 UI；截图留证。
- [ ] **Step 4:** 提交（git cz，Feat）。

---

## 附录 A（暂不执行）: 解耦方法二 —— LVGL v9 Observer/Subject（记录 / 演进参考）

> **本 PR 暂不执行**，仅作记录与将来演进参考。当数据点变多（时间/电量/触感状态…）时，可从“回调注入”平滑演进到 LVGL 原生 Observer；接口边界一致（UI 层只认“数据来自外部”）。参考实现：

```c
/* model 层（可移植）：定义数据主体 */
static lv_subject_t s_time_subject;
static char s_time_buf[32];
static char s_time_prev[32];

void ui_model_init(void)
{
    lv_subject_init_string(&s_time_subject, s_time_buf, s_time_prev,
                           sizeof(s_time_buf), "--:--:--");
}

/* UI 侧：label 绑定 subject，值变自动刷新，widget 删除自动退订 */
static void on_time_changed(lv_observer_t *observer, lv_subject_t *subject)
{
    lv_obj_t *label = lv_observer_get_target_obj(observer);
    lv_label_set_text(label, lv_subject_get_string(subject));
}
/* 在 ui_app_create 内： */
/* lv_subject_add_observer_obj(&s_time_subject, on_time_changed, s_clock_label, NULL); */

/* 数据源侧（真机 RTC / PC stub）：只更新数据，不接触 UI */
/* lv_subject_copy_string(&s_time_subject, real_or_pc_time_string()); */
```

需确保 `LV_USE_OBSERVER` 开启（ESP 侧 menuconfig `CONFIG_LV_USE_OBSERVER`，PC 侧 `lv_conf.h`）。

---

## 提交与 PR 汇总

- 分支：`cursor/hp28008-lvgl-pc-sim-1073`（自 `dev`）。
- 代码/工程提交顺序：Task1 ui_app → Task2 真机接入 →（Task3 验证一般不产生提交）→ Task4 pc_simulator → Task5 说明(README/AGENTS)。**Task6 与附录 A 本 PR 暂不执行。**
- **superpowers 文档提交时机（用户要求）**：`docs/superpowers/` 的本 spec 与 plan **只在全部开发完成（Task 1-5 实现 + 验证通过）之后，作为最后一次 docs 提交**；在此之前仅保留在本地工作区，不 `git add`/`commit`。本轮先行创建的 spec/plan 亦遵循此约定，暂不提交。
- 每个逻辑代码提交后 `git push -u origin cursor/hp28008-lvgl-pc-sim-1073`，创建/更新 draft PR（base `dev`）。
- **Review gate（**`subagent-driven-development` **要求）**：每个实现 Task 提交后，必须先派 spec-reviewer subagent（确认代码与 spec 一致，无多无少），通过后再派 code-quality reviewer subagent（代码质量），两阶段全部通过后才在 TodoWrite 标记完成并进入下一个 Task。

## 后续（本 plan 不含 / 暂不执行）

- Task 6：后端 B（QEMU 240x320 复用同一 UI）。
- 附录 A：解耦方法从回调注入演进到 Observer/Subject 统一 model 层。
- 后端 C（LovyanGFX SDL）统一真机+PC 显示驱动（spec 第 10 节）。
- 浏览器/WASM 分享（lv_web_emscripten / Codespaces）；PC 侧 CI headless 截图存档。

---

## 执行结果

执行日期：2026-07-03。代码提交分支：`cursor/hp28008-lvgl-pc-sim-1073`。

### Task 1 — main/ui_app 可移植 UI 层

- 构建验证：`idf.py build`（含 ui_app/ GLOB 源码）编译通过，无符号冲突，无 v9 API 错误。
- Task1 首个提交（ui_app）已含：回调注入、可移植分辨率宏、`s_clock_timer` 保存 `lv_timer_create` 返回值。
- Code Review 修复（ui_app 的 fix 提交）：① `ui_app_create` 顶部加 `LV_ASSERT_MSG(s_clock_label == NULL)` 防重复调用；② timer 回调去除永不触发的 NULL 检查，改为中文注释说明约束。

### Task 2 — hp28008.c 重构 + CMake 注册

- 构建验证：两步均通过——当前 QEMU demo 编译 OK；临时切换 `main_lvgl.c` 入口后编译 OK（真机链路完整）。
- Code Review Minor 修复：① `real_clock_source()` 在 `strftime` 写入 0 字节时返回 `NULL`（M2）；② `hp28008.c` 文件头补 `SPDX-FileContributor`（M3）。
- 说明：`lv_obj_t *scr = lv_scr_act()` 移至 `lvgl_port_lock(0)` 之后已在 Task2 重构提交完成（M1）。

### Task 3 — 解耦阶段构建验证

- Step 1（当前 QEMU demo 构建）：PASS，2056/2056 steps，固件 54% flash 可用。
- Step 2（真机入口 main_lvgl.c 临时构建）：PASS，增量编译通过，真机链路完整，已切回。

### Task 4 — pc_simulator PC/SDL 模拟工程

- 构建验证：`cmake --build pc_simulator/build` 通过，生成 1.3MB ELF binary。
- Code Review Important 修复：① `cmake_policy(SET CMP0169 OLD)` 消除 FetchContent_Populate 废弃警告；② `lv_conf.h` 补 `LV_LOG_PRINTF 1`；③ CMakeLists.txt/ui_app.c 日文注释替换。
- Code Review Suggestion 修复：④ `LV_USE_BTN` → `LV_USE_BUTTON`；⑤ 原拟入库的 esp_logo C 数组方案改为构建时动态生成（不入库）。
- 追加修复（QA-执行-4/5/6）：⑥ `pc_simulator/main.c` `localtime()` → `localtime_r()`（线程安全）；⑦ CMakeLists.txt 加 `if(POLICY CMP0169)` guard、`find_package(SDL2 CONFIG QUIET)` fallback（SDL2 链接统一为 `PC_SIMULATOR_SDL2_LINK`）、venv sentinel 记录依赖清单并比对（幂等）、路径变量加引号；注释保持中文。

### Task 5 — 文档

- `pc_simulator/README.md` 与 `AGENTS.md` PC 模拟小节均已创建/插入，内容准确。
- Code Review：审查通过，无 Critical/Important 问题。

### QA-执行 记录

1. `LV_USE_BTN`（LVGL v8 旧名）→ `LV_USE_BUTTON`（v9 正名）：`lv_conf.h` 一次性写为 v9 正名，合入 pc_simulator 的 feat 提交。
2. Timer handle 保存至 `static lv_timer_t *s_clock_timer`：Task1 首个提交即包含。
3. `lv_scr_act()` 移至 `lvgl_port_lock()` 锁内：合入 Task2 重构提交。
4. `localtime()` 非线程安全 → `localtime_r()` + 独立 `struct tm tm_buf`：已修复，合入 pc_simulator feat 提交。
5. CMakeLists.txt 注释语言：最终保持中文（日文注释已去除）；加 `if(POLICY CMP0169)` guard、`find_package(SDL2 CONFIG QUIET)` fallback（SDL2 链接统一为 `PC_SIMULATOR_SDL2_LINK`）、venv sentinel 记录依赖清单并比对（幂等）、路径变量加引号。
6. Suggestion（未修复，记录在案）：`lv_img_create → lv_image_create`（LVGL v9 正名，优先级低，与 hp28008.c 既有代码一致，留待 LVGL v10 升级时处理）。

## 缺陷复盘：PC 模拟器旋转后画面错位（systematic-debugging）

**现象**：`pc_simulator` 点击 “Rotate screen” 后画面斜切错位（红色 logo 变斜向条纹、文字变噪点、按钮错位）；0° 显示正常。

**根因**：LVGL v9 的 `lv_display_set_rotation()` 只交换逻辑分辨率、不做实际像素旋转；像素旋转须由 driver 的 flush_cb 用 `lv_draw_sw_rotate()` 完成。LVGL SDL sw backend（`lv_sdl_sw.c`）的 flush_cb **仅在 `LV_SDL_RENDER_MODE == LV_DISPLAY_RENDER_MODE_PARTIAL` 分支** 调用 `lv_draw_sw_rotate`；而 `LV_SDL_RENDER_MODE` 默认是 `LV_DISPLAY_RENDER_MODE_DIRECT`（`lv_conf_internal.h`）。DIRECT 模式下 flush_cb 跳过旋转，但 `resize_display` 已按交换后的分辨率(320x240)重建 SDL texture，fb 数据未旋转 → stride 错配 → 斜切错位。真机不受影响（esp_lvgl_port/ST7789 硬件旋转）。

**修复**：`pc_simulator/lv_conf.h` 显式设 `#define LV_SDL_RENDER_MODE LV_DISPLAY_RENDER_MODE_PARTIAL`（合入 pc_simulator 的 feat 提交）。符合 LVGL 官方“partial rendering 推荐”的旋转做法。

**验证**（`DISPLAY=:1` + `ffmpeg -f x11grab` 截图，临时给 main.c 加自动旋转 90° 复现，验证后已还原）：
- 复现：DIRECT + 旋转 90° → 斜切错位（截图确认）。
- 修复：PARTIAL + 旋转 90° → 画面正常（logo/文字/按钮清晰）。
- 回归：0° 正常显示未破坏。

**构建坑（记录）**：`LV_SDL_RENDER_MODE` 是 LVGL 库的编译期常量；改 `lv_conf.h` 后增量 `cmake --build`（含 `--clean-first`）不会重编由 `add_subdirectory` 引入的 lvgl 库，必须删 `pc_simulator/build/` 全量重建才生效。