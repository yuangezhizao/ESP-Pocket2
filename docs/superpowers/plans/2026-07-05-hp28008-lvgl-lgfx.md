# hp28008 基于 LovyanGFX 的 LVGL 后端 Implementation Plan

> **状态：已实现（分支 `cursor/hp28008-lvgl-lgfx-5194`，base `dev`；经 superpowers subagent-driven-development 执行，7 个 Task 全部完成并通过构建验证，详见文末「执行结果」；plan/spec 随 PR9 的最后一次提交入库）。**

> **For agentic workers:** REQUIRED SUB-SKILL：用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐个 Task 实现。步骤用 `- [ ]` 跟踪，执行后回填「执行结果」。Task tool 不指定 model 让 subagent 继承父模型（本仓库要求父子模型一致）。

**Goal:** 并列新增一条基于 LovyanGFX 的 LVGL 路径——真机入口 `main_lvgl_lgfx.c`（LGFX 驱动 HP28008 显示 + esp_lcd_touch 触摸 + 真实 RTC）与 PC 工程 `pc_simulator_lgfx/`（LGFX `Panel_sdl` 后端 + 鼠标触摸），两端共用一份可移植 bridge 与同一份 `main/ui_app/`；不改动 `main_lvgl.c` / `hp28008.c` / `pc_simulator/`（仅新增工程/入口，并更新 `AGENTS.md` 文档指引）。

**Architecture:** 三层——`ui_app`（纯 UI）→ `lvgl_lgfx_bridge`（可移植：仅依赖 LVGL + LovyanGFX + `ui_app.h`，负责建 `lv_display`/`lv_indev` + `flush_cb`(`pushImageDMA`+`setRotation`)/`read_cb`(注入触摸源)) → 各平台适配（真机 `lvgl_lgfx_hp28008.cpp` 自管 LVGL 运行时 + `esp_lcd_touch_gt911`；PC `main.cpp` 用 `Panel_sdl::main`）。LGFX 只做显示（SPI，不碰 I2C），I2C 全交项目 esp-idf 驱动。

**Tech Stack:** ESP-IDF v5.5.4 (esp32s3)、LVGL v9.5.0、LovyanGFX 1.2.9（submodule）、SDL2、CMake。

**设计依据：** [docs/superpowers/specs/2026-07-05-hp28008-lvgl-lgfx-design.md](https://github.com/yuangezhizao/ESP-Pocket2/blob/dev/docs/superpowers/specs/2026-07-05-hp28008-lvgl-lgfx-design.md)（决策 D1–D11 / QA 均见此）。

---

## Task 1: 提取 LGFX 设备类 `lgfx_hp28008.hpp` + 改 `lgfx.cpp`（收编全局 count）

**Files:**
- Create: `main/lgfx/lgfx_hp28008.hpp`
- Modify: `main/lgfx/lgfx.cpp`

- [x] **Step 1:** 创建 `main/lgfx/lgfx_hp28008.hpp`（从 `lgfx.cpp` 抽取 LGFX 设备类，改名 `LGFX_HP28008`，加 `with_touch` 构造参数控制是否 `setTouch`；引脚用宏、禁止裸魔数）。

```cpp
/*
 * SPDX-FileCopyrightText: 2026 ESP-Pocket2
 *
 * SPDX-License-Identifier: Apache-2.0
 *
 * HP28008（ST7789 SPI 240x320 + GT911）的 LovyanGFX 设备类。
 * with_touch=true（默认）供 lgfx_test() 用；with_touch=false 供真机 LVGL 入口用
 * （不 setTouch → LGFX 不碰 I2C，触摸改走 esp_lcd_touch，见 spec 第 3 节 I2C 冲突约束）。
 * 引脚与 main/lvgl/hp28008.h 的 EXAMPLE_LCD_* / EXAMPLE_TOUCH_* 约定同值（唯 touch I2C port 例外，见 HP28008_LGFX_TOUCH_I2C_PORT 处说明）。
 */
#pragma once

#define LGFX_USE_V1
#include <LovyanGFX.hpp>

#define HP28008_LGFX_SPI_HOST       SPI3_HOST
#define HP28008_LGFX_PIN_SCLK       GPIO_NUM_12
#define HP28008_LGFX_PIN_MOSI       GPIO_NUM_11
#define HP28008_LGFX_PIN_MISO       GPIO_NUM_13
#define HP28008_LGFX_PIN_DC         GPIO_NUM_9
#define HP28008_LGFX_PIN_CS         GPIO_NUM_10
#define HP28008_LGFX_PIN_RST        GPIO_NUM_3
#define HP28008_LGFX_PIN_BL         GPIO_NUM_46
/* 面板尺寸：本头保持零 LVGL 依赖（供纯 LovyanGFX 的 lgfx_test 复用），故独立定义；
 * 与 ui_app.h 的 UI_APP_HOR_RES/VER_RES、hp28008.h 的 EXAMPLE_LCD_H_RES/V_RES 三处约定同值(240x320)。 */
#define HP28008_LGFX_PANEL_W        240
#define HP28008_LGFX_PANEL_H        320
#define HP28008_LGFX_SPI_FREQ_WRITE 40000000
#define HP28008_LGFX_SPI_FREQ_READ  16000000
#define HP28008_LGFX_BL_PWM_CH      7
#define HP28008_LGFX_BL_PWM_FREQ    44100
#define HP28008_LGFX_TOUCH_SDA      GPIO_NUM_47
#define HP28008_LGFX_TOUCH_SCL      GPIO_NUM_48
#define HP28008_LGFX_TOUCH_INT      GPIO_NUM_45
/* touch I2C port 沿用现有 lgfx.cpp 的 1（仅 with_touch=true 的 lgfx_test 用；真机 LVGL 入口 with_touch=false，
 * 触摸走 esp_lcd_touch 的项目 bus=port 0，不经此）。故此项与 hp28008.h 的 EXAMPLE_TOUCH_I2C_NUM=0 不同值；
 * 其余触摸引脚(SDA/SCL/INT/ADDR/FREQ)与 hp28008.h 同值。 */
#define HP28008_LGFX_TOUCH_I2C_PORT 1
#define HP28008_LGFX_TOUCH_ADDR     0x5D
#define HP28008_LGFX_TOUCH_FREQ     400000

class LGFX_HP28008 : public lgfx::LGFX_Device
{
    lgfx::Panel_ST7789 _panel_instance;
    lgfx::Bus_SPI      _bus_instance;
    lgfx::Light_PWM    _light_instance;
    lgfx::Touch_GT911  _touch_instance;

public:
    explicit LGFX_HP28008(bool with_touch = true)
    {
        {
            auto cfg = _bus_instance.config();
            cfg.spi_host = HP28008_LGFX_SPI_HOST;
            cfg.spi_mode = 0;
            cfg.freq_write = HP28008_LGFX_SPI_FREQ_WRITE;
            cfg.freq_read = HP28008_LGFX_SPI_FREQ_READ;
            cfg.spi_3wire = true;
            cfg.use_lock = true;
            cfg.dma_channel = SPI_DMA_CH_AUTO;
            cfg.pin_sclk = HP28008_LGFX_PIN_SCLK;
            cfg.pin_mosi = HP28008_LGFX_PIN_MOSI;
            cfg.pin_miso = HP28008_LGFX_PIN_MISO;
            cfg.pin_dc = HP28008_LGFX_PIN_DC;
            _bus_instance.config(cfg);
            _panel_instance.setBus(&_bus_instance);
        }
        {
            auto cfg = _panel_instance.config();
            cfg.pin_cs = HP28008_LGFX_PIN_CS;
            cfg.pin_rst = HP28008_LGFX_PIN_RST;
            cfg.pin_busy = GPIO_NUM_NC;
            cfg.panel_width = HP28008_LGFX_PANEL_W;
            cfg.panel_height = HP28008_LGFX_PANEL_H;
            cfg.offset_x = 0;
            cfg.offset_y = 0;
            cfg.offset_rotation = 0;
            cfg.dummy_read_pixel = 8;
            cfg.dummy_read_bits = 1;
            cfg.readable = true;
            /* invert=false 沿用现有 lgfx.cpp 的 LovyanGFX 配置（其 ST7789 默认极性与 esp_lcd 不同）；
             * 与 hp28008.h 的 EXAMPLE_LCD_INVERT_COLOR=1 语义不同源、勿直接对齐，真机若颜色反相再调。 */
            cfg.invert = false;
            cfg.rgb_order = true;
            cfg.dlen_16bit = false;
            cfg.bus_shared = true;
            _panel_instance.config(cfg);
        }
        {
            auto cfg = _light_instance.config();
            cfg.pin_bl = HP28008_LGFX_PIN_BL;
            cfg.invert = false;
            cfg.freq = HP28008_LGFX_BL_PWM_FREQ;
            cfg.pwm_channel = HP28008_LGFX_BL_PWM_CH;
            _light_instance.config(cfg);
            _panel_instance.setLight(&_light_instance);
        }
        if (with_touch)
        {
            auto cfg = _touch_instance.config();
            cfg.x_min = 0;
            cfg.x_max = HP28008_LGFX_PANEL_W - 1;
            cfg.y_min = 0;
            cfg.y_max = HP28008_LGFX_PANEL_H - 1;
            cfg.pin_int = HP28008_LGFX_TOUCH_INT;
            cfg.bus_shared = true;
            cfg.offset_rotation = 0;
            cfg.i2c_port = HP28008_LGFX_TOUCH_I2C_PORT;
            cfg.i2c_addr = HP28008_LGFX_TOUCH_ADDR;
            cfg.pin_sda = HP28008_LGFX_TOUCH_SDA;
            cfg.pin_scl = HP28008_LGFX_TOUCH_SCL;
            cfg.freq = HP28008_LGFX_TOUCH_FREQ;
            _touch_instance.config(cfg);
            _panel_instance.setTouch(&_touch_instance);
        }
        setPanel(&_panel_instance);
    }
};
```

- [x] **Step 2:** 改 `main/lgfx/lgfx.cpp`，共 4 处机械编辑（`lgfx_test()` 逻辑不变）：(1) 顶部删 `#define LGFX_USE_V1` 与 `#include <LovyanGFX.hpp>`，改为 `#include "lgfx_hp28008.hpp"`（该头已带这两者）；(2) 删除整个 `class LGFX : public lgfx::LGFX_Device { ... };` 定义；(3) 删文件级 `uint32_t count = ~0;`，实例类型 `LGFX`→`LGFX_HP28008`；(4) 在 `lgfx_test()` 首行插入 `uint32_t count = ~0;`。改后完整文件如下（原文件中那段被 `//` 注释掉的示例循环予以**删除、不保留**，与下方完整文件一致；`lgfx_test()` 逻辑不变）：

```cpp
#include "lgfx.h"
#include "lgfx_hp28008.hpp"

// Refer: https://web.archive.org/web/20250918152441/https://blog.csdn.net/weixin_41589183/article/details/137756695

static LGFX_HP28008 lcd;
static LGFX_Sprite sprite(&lcd);

void lgfx_test(void)
{
    uint32_t count = ~0;   // 原为文件级全局符号，收编进函数内消除符号污染
    lcd.init();
    lcd.setColorDepth(1);
    sprite.setColorDepth(1);
    lcd.setTextSize((std::max(lcd.width(), lcd.height()) + 255) >> 8);
    lcd.fillScreen(TFT_BLACK);

    while (1)
    {
        lcd.startWrite();
        lcd.setRotation(++count & 7);
        lcd.setColorDepth((count & 8) ? 16 : 24);

        lcd.setTextColor(TFT_BLACK);
        lcd.drawNumber(lcd.getRotation(), 16, 0);

        lcd.setTextColor(0xFF0000U);
        lcd.drawString("R", 25, 16);
        lcd.setTextColor(0x00FF00U);
        lcd.drawString("G", 32, 16);
        lcd.setTextColor(0x0000FFU);
        lcd.drawString("B", 39, 16);
        lcd.setTextColor(0x0467FFU);
        lcd.drawString("JCZN", 25, 3);
        lcd.drawRect(30, 30, lcd.width() - 60, lcd.height() - 60, count * 7);
        lcd.drawFastHLine(0, 0, 10);

        lcd.endWrite();

        int32_t x, y;
        if (lcd.getTouch(&x, &y))
        {
            lcd.fillRect(x - 2, y - 2, 5, 5, count * 7);
        }

        vTaskDelay(pdMS_TO_TICKS(10));
    }
}
```

- [x] **Step 3:** 构建验证（当前生效入口仍是 `main_lvgl_qemu_rgb.c`，但 `lgfx/*.cpp` 属组件 GLOB 会一并编译）。

Run: `source /opt/esp/idf/export.sh && idf.py build`
Expected: 编译通过；`lgfx.cpp` 用 `LGFX_HP28008` 无错、无 `count` 重复定义符号。

- [x] **Step 4:** 提交（git cz 格式）。

```bash
git add main/lgfx/lgfx_hp28008.hpp main/lgfx/lgfx.cpp
git commit -m "$(cat <<'EOF'
refactor(main/lgfx): ♻️ 提取 LGFX_HP28008 设备类并收编全局 count

- 新增 lgfx_hp28008.hpp：HP28008 的 LovyanGFX 设备类，with_touch 参数控制是否 setTouch
  （真机 LVGL 入口将传 false，使 LGFX 不碰 I2C，触摸走 esp_lcd_touch）
- lgfx.cpp 改 include 该头、实例改 LGFX_HP28008，全局 count 收进 lgfx_test() 内部
- 引脚全部宏定义，禁止裸魔数；lgfx_test() 行为不变
EOF
)"
```

---

## Task 2: 可移植 bridge `lvgl_lgfx_bridge.{hpp,cpp}`

**Files:**
- Create: `main/lvgl_lgfx/lvgl_lgfx_bridge.hpp`
- Create: `main/lvgl_lgfx/lvgl_lgfx_bridge.cpp`

- [x] **Step 1:** 创建 `main/lvgl_lgfx/lvgl_lgfx_bridge.hpp`（可移植接口，仅依赖 LVGL + LovyanGFX + `ui_app.h`；`LVGL_LGFX_DRAW_BUFF_HEIGHT` 单一来源定义于此）。

```cpp
/*
 * SPDX-FileCopyrightText: 2026 ESP-Pocket2
 *
 * SPDX-License-Identifier: Apache-2.0
 *
 * 可移植 LVGL <-> LovyanGFX 桥接：建 lv_display/lv_indev + flush/read 回调。
 * 仅依赖 lvgl + LovyanGFX + ui_app.h，绝不依赖 esp-idf，真机与 PC 共用同一份。
 * lv_init / tick / lv_timer_handler 循环由各入口负责（平台相关运行时）。
 */
#pragma once

#define LGFX_USE_V1
#include <LovyanGFX.hpp>
#include "lvgl.h"
#include "ui_app.h"

/* draw buffer 高度（行），真机/PC 各自据此分配缓冲；禁止裸魔数（见 spec 编码规范） */
#define LVGL_LGFX_DRAW_BUFF_HEIGHT 50

/* 本头为纯 C++（cfg 含 lgfx:: 类型），仅供 .cpp include、不提供 C 接口，故不加 extern "C"。
 * 真正的 C↔C++ 边界是 lvgl_lgfx_hp28008.cpp 导出的 extern "C" lvgl_lgfx_hp28008_run()。 */

/* 触摸源：返回是否按下；按下时填「逻辑坐标系」的 x/y。由各平台注入。 */
typedef bool (*lvgl_lgfx_touch_read_fn)(int32_t *x, int32_t *y);

typedef struct {
    lgfx::LGFX_Device *lcd;            /* 注入的 LGFX 设备（真机 ST7789 / PC Panel_sdl） */
    void *buf1;                        /* draw buffer 1（各入口分配） */
    void *buf2;                        /* draw buffer 2（双缓冲） */
    uint32_t buf_size_bytes;           /* 每块字节数 */
    lvgl_lgfx_touch_read_fn touch_read; /* 触摸源，可为 NULL */
    ui_clock_source_fn clock_src;       /* 时钟源，可为 NULL */
} lvgl_lgfx_bridge_cfg_t;

/*
 * 建 lv_display + lv_indev + flush/read cb + ui_app_create（内部按 UI_APP_HOR_RES/VER_RES）。
 * 前置：调用方须已 lv_init()；若平台有独立 LVGL task/锁（真机）须在 LVGL 锁内调用；PC 单线程 setup 阶段无需额外锁。
 * 返回创建的 lv_display_t*（供入口读取旋转等）。
 */
lv_display_t *lvgl_lgfx_bridge_create(const lvgl_lgfx_bridge_cfg_t *cfg);
```

- [x] **Step 2:** 创建 `main/lvgl_lgfx/lvgl_lgfx_bridge.cpp`（flush_cb：`pushImageDMA` + 旋转跟随；read_cb：调注入的触摸源）。

```cpp
/*
 * SPDX-FileCopyrightText: 2026 ESP-Pocket2
 *
 * SPDX-License-Identifier: Apache-2.0
 */
#include "lvgl_lgfx_bridge.hpp"

static lvgl_lgfx_touch_read_fn s_touch_read = NULL;

static void lvgl_lgfx_flush_cb(lv_display_t *disp, const lv_area_t *area, uint8_t *px_map)
{
    lgfx::LGFX_Device *lcd = (lgfx::LGFX_Device *)lv_display_get_user_data(disp);

    /* G7-B：把 LVGL 逻辑旋转同步到 LGFX（真机=ST7789 MADCTL 硬件旋转；PC=Panel_sdl 坐标映射）。
     * 注：LVGL v9 的 lv_display_set_rotation 只交换内部分辨率、不旋转像素（官方 rotation 文档），
     * 故实际像素旋转由此处 LGFX setRotation 承担，本方案刻意不调用 lv_draw_sw_rotate（省一次软件旋转拷贝）。
     * lv_display_rotation_t 的 0/90/180/270 枚举值即 0..3，与 LGFX setRotation 的 0..3 对应。 */
    static lv_display_rotation_t s_last_rot = LV_DISPLAY_ROTATION_0;
    lv_display_rotation_t rot = lv_display_get_rotation(disp);
    if (rot != s_last_rot) {
        s_last_rot = rot;
        lcd->setRotation((uint_fast8_t)rot);
    }

    const int32_t w = area->x2 - area->x1 + 1;
    const int32_t h = area->y2 - area->y1 + 1;
    lcd->startWrite();
    lcd->pushImageDMA(area->x1, area->y1, w, h, (lgfx::rgb565_t *)px_map);
    lcd->endWrite();
    lv_display_flush_ready(disp);
}

static void lvgl_lgfx_read_cb(lv_indev_t *indev, lv_indev_data_t *data)
{
    LV_UNUSED(indev);
    int32_t x = 0, y = 0;
    if (s_touch_read != NULL && s_touch_read(&x, &y)) {
        data->point.x = x;
        data->point.y = y;
        data->state = LV_INDEV_STATE_PRESSED;
    } else {
        data->state = LV_INDEV_STATE_RELEASED;
    }
}

lv_display_t *lvgl_lgfx_bridge_create(const lvgl_lgfx_bridge_cfg_t *cfg)
{
    LV_ASSERT_NULL(cfg);
    LV_ASSERT_NULL(cfg->lcd);
    LV_ASSERT_NULL(cfg->buf1);

    lv_display_t *disp = lv_display_create(UI_APP_HOR_RES, UI_APP_VER_RES);
    if (disp == NULL) {
        return NULL;
    }
    lv_display_set_color_format(disp, LV_COLOR_FORMAT_RGB565);
    lv_display_set_user_data(disp, cfg->lcd);
    lv_display_set_flush_cb(disp, lvgl_lgfx_flush_cb);
    lv_display_set_buffers(disp, cfg->buf1, cfg->buf2, cfg->buf_size_bytes,
                           LV_DISPLAY_RENDER_MODE_PARTIAL);

    s_touch_read = cfg->touch_read;
    lv_indev_t *indev = lv_indev_create();
    if (indev != NULL) {
        lv_indev_set_type(indev, LV_INDEV_TYPE_POINTER);
        lv_indev_set_display(indev, disp);
        lv_indev_set_read_cb(indev, lvgl_lgfx_read_cb);
    } else {
        LV_LOG_WARN("lv_indev_create failed; display works but input is disabled");
    }

    ui_app_create(lv_display_get_screen_active(disp), cfg->clock_src);
    return disp;
}
```

- [x] **Step 3:** 提交（构建验证放 Task 3 与 CMake 注册一起做）。

```bash
git add main/lvgl_lgfx/lvgl_lgfx_bridge.hpp main/lvgl_lgfx/lvgl_lgfx_bridge.cpp
git commit -m "$(cat <<'EOF'
feat(main/lvgl_lgfx): ✨ 新增可移植 LVGL<->LovyanGFX bridge

- lvgl_lgfx_bridge_create：建 lv_display/lv_indev + flush/read 回调 + ui_app_create
- flush_cb：pushImageDMA(rgb565_t) + 旋转跟随 setRotation（G6 档2 + G7-B）
- read_cb：调注入的触摸源；触摸/时钟均由各入口注入，bridge 不依赖 esp-idf
- LVGL_LGFX_DRAW_BUFF_HEIGHT 单一来源；分辨率用 UI_APP_HOR_RES/VER_RES 宏
EOF
)"
```

---

## Task 3: 真机适配 `lvgl_lgfx_hp28008.cpp` + 入口 `main_lvgl_lgfx.c` + CMake 注册

**Files:**
- Create: `main/lvgl_lgfx/lvgl_lgfx_hp28008.cpp`
- Create: `main/main_lvgl_lgfx.c`
- Modify: `main/CMakeLists.txt`
- Modify: `main/idf_component.yml`

- [x] **Step 1:** 创建 `main/lvgl_lgfx/lvgl_lgfx_hp28008.cpp`（真机：建 `LGFX_HP28008(false)` + `esp_lcd_touch_gt911` 复用 `bus_handle` + 真实 RTC 时钟源 + 触摸旋转变换 + 自管 LVGL 运行时；导出 C 接口）。

```cpp
/*
 * SPDX-FileCopyrightText: 2026 ESP-Pocket2
 *
 * SPDX-License-Identifier: Apache-2.0
 *
 * 真机适配：LGFX 只做显示（不 setTouch），触摸走 esp_lcd_touch_gt911（复用项目 bus_handle），
 * 时钟走 BM8563 RTC；自管 LVGL 运行时（esp_timer tick + FreeRTOS task + recursive mutex）。
 * 不改动 main/lvgl/hp28008.c。
 */
#include "lvgl_lgfx_bridge.hpp"
#include "lgfx_hp28008.hpp"

/* 各头自带 extern "C" 边界，C++ 文件普通 include 即可，切勿外包 extern "C"：
 * i2c_app.h 在 __cplusplus 下会引入纯 C++ 的 RtcDrv.hpp/rtc，且 RTC_global_variable 在 extern "C" 外，
 * 外包会把它们拖入 C linkage → 编译失败(namespace/STL) 或链接冲突。现有 i2c_app.cpp 亦是普通 include。 */
#include "esp_err.h"
#include "esp_log.h"
#include "esp_check.h"   /* ESP_RETURN_ON_ERROR / ESP_RETURN_ON_FALSE */
#include "esp_timer.h"
#include "esp_heap_caps.h"
#include "freertos/FreeRTOS.h"
#include "freertos/task.h"
#include "freertos/semphr.h"
#include "driver/i2c_master.h"
#include "esp_lcd_panel_io.h"
#include "esp_lcd_touch_gt911.h"
#include "i2c_app.h"   /* update_RTC_global_variable / RTC_global_variable */

/* 本入口依赖项目 i2c_driver 的 bus_handle（GT911 复用），该符号仅在 SensorLib 新 API 下定义。
 * 注意：main 组件对各子目录是「全量 GLOB 编译」（见 main/CMakeLists.txt），本文件无论当前生效入口
 * 是谁都会被编译；故不能用顶层 #error（否则 old API 配置会波及所有入口的构建），改为：新 API 下编译
 * 完整实现，old API 下编译一个返回 ESP_ERR_NOT_SUPPORTED 的 stub（见文件末尾 #else 分支）。 */
#if defined(CONFIG_SENSORLIB_ESP_IDF_NEW_API)

static const char *TAG = "LVGL-LGFX-HP28008";

#define LVGL_LGFX_TICK_PERIOD_MS   2
#define LVGL_LGFX_TASK_STACK_SIZE  (8 * 1024)
#define LVGL_LGFX_TASK_PRIORITY    4
#define LVGL_LGFX_TASK_MAX_DELAY   500
#define LVGL_LGFX_TASK_MIN_DELAY   1
#define LVGL_LGFX_TOUCH_RST_GPIO   GPIO_NUM_21
#define LVGL_LGFX_TOUCH_INT_GPIO   GPIO_NUM_45
#define LVGL_LGFX_TOUCH_CLK_HZ     400000
#define LVGL_LGFX_BL_BRIGHTNESS    128   /* 0..255，约 50% */

/* 必须 with_touch=false：真机 GT911 走 esp_lcd_touch/项目 bus，LGFX 绝不能再 init I2C，
 * 否则重现 spec 第 3 节的新旧驱动同总线冲突。切勿改用默认构造。 */
static LGFX_HP28008 s_lcd(false);
static esp_lcd_touch_handle_t s_touch = NULL;
static lv_display_t *s_disp = NULL;
static SemaphoreHandle_t s_lvgl_mux = NULL;

/* 触摸源：读 esp_lcd_touch（物理坐标），按当前 LVGL 旋转变换到逻辑坐标。
 * 旋转方向为标准 CW 假设，真机实测若镜像相反再调（见 spec 第 9 节验证）。 */
static bool real_touch_read(int32_t *x, int32_t *y)
{
    /* esp_lcd_touch 1.2.1 起 deprecate get_coordinates 并新增 get_data（返回 esp_err_t，触点数看 cnt）；
     * 本 plan 已在 idf_component.yml 约束底层 esp_lcd_touch ^1.2.1 保证 get_data 可用（见 Task 3 Step 3）。
     * esp_lcd_touch_point_data_t 的字段(.x/.y)以实际拉取的组件头为准（见验证策略「API 核对」）。 */
    if (esp_lcd_touch_read_data(s_touch) != ESP_OK) {
        return false;   /* I2C 瞬时读失败：本帧视为无触摸、下帧重试，好过沿用内部旧数据 */
    }
    esp_lcd_touch_point_data_t points[1] = {};
    uint8_t cnt = 0;
    if (esp_lcd_touch_get_data(s_touch, points, &cnt, 1) != ESP_OK || cnt == 0) {
        return false;
    }
    const int32_t tx = points[0].x;
    const int32_t ty = points[0].y;
    switch (lv_display_get_rotation(s_disp)) {
    case LV_DISPLAY_ROTATION_90:
        *x = ty;
        *y = (UI_APP_HOR_RES - 1) - tx;
        break;
    case LV_DISPLAY_ROTATION_180:
        *x = (UI_APP_HOR_RES - 1) - tx;
        *y = (UI_APP_VER_RES - 1) - ty;
        break;
    case LV_DISPLAY_ROTATION_270:
        *x = (UI_APP_VER_RES - 1) - ty;
        *y = tx;
        break;
    default:
        *x = tx;
        *y = ty;
        break;
    }
    return true;
}

/* 时钟源：读 BM8563 RTC（不改 hp28008.c，此处独立定义；刷新失败返回 NULL） */
static const char *real_clock_source(void)
{
    return update_RTC_global_variable() ? RTC_global_variable : NULL;
}

static void lvgl_tick_cb(void *arg)
{
    LV_UNUSED(arg);
    lv_tick_inc(LVGL_LGFX_TICK_PERIOD_MS);
}

static void lvgl_task(void *arg)
{
    LV_UNUSED(arg);
    uint32_t delay_ms = LVGL_LGFX_TASK_MAX_DELAY;
    while (1) {
        if (xSemaphoreTakeRecursive(s_lvgl_mux, portMAX_DELAY) == pdTRUE) {
            delay_ms = lv_timer_handler();
            xSemaphoreGiveRecursive(s_lvgl_mux);
        }
        if (delay_ms > LVGL_LGFX_TASK_MAX_DELAY) delay_ms = LVGL_LGFX_TASK_MAX_DELAY;
        else if (delay_ms < LVGL_LGFX_TASK_MIN_DELAY) delay_ms = LVGL_LGFX_TASK_MIN_DELAY;
        vTaskDelay(pdMS_TO_TICKS(delay_ms));
    }
}

extern "C" esp_err_t lvgl_lgfx_hp28008_run(void)
{
    ESP_LOGI(TAG, "Init LGFX display (ST7789 SPI, display-only)");
    /* LGFX_Device::init() 返回 bool；ST7789(Panel_LCD) 实际恒真，此检查为防御性/与本文件其他初始化一致 */
    ESP_RETURN_ON_FALSE(s_lcd.init(), ESP_FAIL, TAG, "LGFX init failed");
    /* 显式 16bpp RGB565：与 bridge 的 (rgb565_t*) flush、spec 色深前提及 PC 侧 setColorDepth(16) 对称。
     * LGFX Panel 默认 _write_depth 虽即 rgb565_2Byte，仍显式声明，不留隐式默认值依赖。 */
    s_lcd.setColorDepth(16);
    s_lcd.setBrightness(LVGL_LGFX_BL_BRIGHTNESS);

    ESP_LOGI(TAG, "Init GT911 touch via esp_lcd_touch (reuse project bus_handle)");
    extern i2c_master_bus_handle_t bus_handle;   /* 由 main_lvgl_lgfx.c 的 i2c_drv_init 建立 */
    esp_lcd_panel_io_handle_t tp_io = NULL;
    /* 逐字段赋值（不直接用 ESP_LCD_TOUCH_IO_I2C_GT911_CONFIG() 宏聚合初始化）：该宏是 designated-initializer 且字段序（.scl_speed_hz 在 .dev_addr 前）与 esp_lcd_panel_io_i2c_config_t 声明序不一致，C++ 严格要求二者顺序一致故直接用会编译失败（C 允许乱序，hp28008.c 是 .c 文件故无碍）；与本文件 tp_cfg 一样用 {} + 逐字段赋值规避，字段值取自该宏（scl_speed_hz 沿用下方覆盖值）。 */
    esp_lcd_panel_io_i2c_config_t tp_io_cfg = {};
    tp_io_cfg.dev_addr = ESP_LCD_TOUCH_IO_I2C_GT911_ADDRESS;
    tp_io_cfg.control_phase_bytes = 1;
    tp_io_cfg.dc_bit_offset = 0;
    tp_io_cfg.lcd_cmd_bits = 16;
    tp_io_cfg.flags.disable_control_phase = 1;
    tp_io_cfg.scl_speed_hz = LVGL_LGFX_TOUCH_CLK_HZ;
    ESP_RETURN_ON_ERROR(esp_lcd_new_panel_io_i2c(bus_handle, &tp_io_cfg, &tp_io), TAG, "touch io failed");

    esp_lcd_touch_config_t tp_cfg = {};
    tp_cfg.x_max = UI_APP_HOR_RES;
    tp_cfg.y_max = UI_APP_VER_RES;
    tp_cfg.rst_gpio_num = LVGL_LGFX_TOUCH_RST_GPIO;
    tp_cfg.int_gpio_num = LVGL_LGFX_TOUCH_INT_GPIO;
    tp_cfg.levels.reset = 0;
    tp_cfg.levels.interrupt = 0;
    ESP_RETURN_ON_ERROR(esp_lcd_touch_new_i2c_gt911(tp_io, &tp_cfg, &s_touch), TAG, "gt911 failed");

    ESP_LOGI(TAG, "Init LVGL + bridge");
    lv_init();
    const uint32_t buf_bytes = UI_APP_HOR_RES * LVGL_LGFX_DRAW_BUFF_HEIGHT * sizeof(lgfx::rgb565_t);
    void *buf1 = heap_caps_malloc(buf_bytes, MALLOC_CAP_DMA | MALLOC_CAP_8BIT);
    void *buf2 = heap_caps_malloc(buf_bytes, MALLOC_CAP_DMA | MALLOC_CAP_8BIT);
    ESP_RETURN_ON_FALSE(buf1 && buf2, ESP_ERR_NO_MEM, TAG, "draw buffer alloc failed");
    /* demo 一次性初始化：任一步失败即返回 esp_err_t 由上层 ESP_ERROR_CHECK 触发重启，
     * 故未对已分配的 buf1/tp_io 做回收（进程重启释放），可接受。 */

    s_lvgl_mux = xSemaphoreCreateRecursiveMutex();
    ESP_RETURN_ON_FALSE(s_lvgl_mux, ESP_ERR_NO_MEM, TAG, "create mutex failed");

    xSemaphoreTakeRecursive(s_lvgl_mux, portMAX_DELAY);
    lvgl_lgfx_bridge_cfg_t cfg = {};
    cfg.lcd = &s_lcd;
    cfg.buf1 = buf1;
    cfg.buf2 = buf2;
    cfg.buf_size_bytes = buf_bytes;
    cfg.touch_read = real_touch_read;
    cfg.clock_src = real_clock_source;
    s_disp = lvgl_lgfx_bridge_create(&cfg);
    xSemaphoreGiveRecursive(s_lvgl_mux);
    ESP_RETURN_ON_FALSE(s_disp, ESP_ERR_NO_MEM, TAG, "bridge create failed");

    esp_timer_create_args_t tick_args = {};   /* 逐字段赋值，与上面 tp_cfg 风格统一，避免 C++ designated-init 顺序问题 */
    tick_args.callback = &lvgl_tick_cb;
    tick_args.name = "lvgl_lgfx_tick";
    esp_timer_handle_t tick_timer = NULL;
    ESP_ERROR_CHECK(esp_timer_create(&tick_args, &tick_timer));
    ESP_ERROR_CHECK(esp_timer_start_periodic(tick_timer, LVGL_LGFX_TICK_PERIOD_MS * 1000));

    BaseType_t ok = xTaskCreate(lvgl_task, "lvgl_lgfx", LVGL_LGFX_TASK_STACK_SIZE, NULL,
                                LVGL_LGFX_TASK_PRIORITY, NULL);
    ESP_RETURN_ON_FALSE(ok == pdPASS, ESP_ERR_NO_MEM, TAG, "create lvgl task failed");
    return ESP_OK;
}

#else  /* !CONFIG_SENSORLIB_ESP_IDF_NEW_API */

/* old API 配置：无 i2c_driver 的 bus_handle，无法复用总线驱动 GT911；导出 stub 保证 main 组件在 SensorLib
 * 新旧 API 任一配置下都能编译（不破坏全量 GLOB 下的其它入口），仅当真把生效入口切到 main_lvgl_lgfx 才在运行时返回不支持。 */
extern "C" esp_err_t lvgl_lgfx_hp28008_run(void)
{
    ESP_LOGE("LVGL-LGFX-HP28008",
             "main_lvgl_lgfx requires CONFIG_SENSORLIB_ESP_IDF_NEW_API=y to reuse i2c_driver bus_handle for GT911");
    return ESP_ERR_NOT_SUPPORTED;
}

#endif  /* CONFIG_SENSORLIB_ESP_IDF_NEW_API */
```

> 注：`esp_lcd_panel_io_i2c_config_t tp_io_cfg = {};` 与 `esp_lcd_touch_config_t tp_cfg = {};` 均用逐字段赋值（避免 C++ designated-initializer 顺序问题——GT911 的 `ESP_LCD_TOUCH_IO_I2C_GT911_CONFIG()` 宏字段序 `.scl_speed_hz` 先于 `.dev_addr`，与结构体声明序相反，C++ 下直接聚合初始化会报 designator order 错，C 则允许，故 `hp28008.c` 是 `.c` 无碍）。`ESP_RETURN_ON_ERROR/ON_FALSE` 由上面已 include 的 `esp_check.h` 提供。`extern i2c_master_bus_handle_t bus_handle;` 引用的是 `i2c_driver.cpp` 的全局（普通全局变量名不 mangling，C/C++ 链接一致，与 `hp28008.c` 的用法相同）。

- [x] **Step 2:** 创建 `main/main_lvgl_lgfx.c`（C 入口：I2C/PMU/RTC 初始化，与 `main_lvgl.c` 一致，再调 run）。

```c
/*
 * SPDX-FileCopyrightText: 2026 ESP-Pocket2
 *
 * SPDX-License-Identifier: Apache-2.0
 *
 * 真机 LVGL-on-LovyanGFX demo 入口：I2C/PMU/RTC 初始化后，用 LGFX 驱动 HP28008 渲染 ui_app。
 * 切换 demo 见 main/CMakeLists.txt 的 SRCS 注释。
 */
#include "esp_err.h"
#include "esp_log.h"
#include "esp_idf_version.h"
#include "i2c_driver.h"
#include "axp202_port.h"
#include "sensor_bm8563.h"   /* bm8563_init（不依赖 esp_lcd 栈的 hp28008.h） */

#if defined(CONFIG_XPOWERS_ESP_IDF_NEW_API) && !defined(CONFIG_SENSORLIB_ESP_IDF_NEW_API)
#include "i2c_port.h"
#endif

esp_err_t lvgl_lgfx_hp28008_run(void);   /* 实现于 lvgl_lgfx_hp28008.cpp */

static const char *TAG = "MAIN-LVGL-LGFX";

void app_main(void)
{
#if CONFIG_I2C_COMMUNICATION_METHOD_BUILTIN_RW || CONFIG_I2C_COMMUNICATION_METHOD_CALLBACK_RW

#if CONFIG_I2C_COMMUNICATION_METHOD_CALLBACK_RW || \
    ((ESP_IDF_VERSION >= ESP_IDF_VERSION_VAL(5, 0, 0)) && \
     (defined(CONFIG_XPOWERS_ESP_IDF_NEW_API) || defined(CONFIG_SENSORLIB_ESP_IDF_NEW_API)))
#if defined(CONFIG_SENSORLIB_ESP_IDF_NEW_API)
    ESP_ERROR_CHECK(i2c_drv_init());
#elif defined(CONFIG_XPOWERS_ESP_IDF_NEW_API)
    ESP_ERROR_CHECK(i2c_init());
#endif
#endif

    ESP_LOGI(TAG, "I2C initialized successfully");
    ESP_ERROR_CHECK(axp202_init());
    i2c_drv_scan();
    ESP_ERROR_CHECK(bm8563_init());

#endif

    ESP_ERROR_CHECK(lvgl_lgfx_hp28008_run());
}
```

> 注：本入口**不** include `hp28008.h`（那是 esp_lcd 栈的头），只用 `i2c_driver.h`/`axp202_port.h`/`sensor_bm8563.h` 做 I2C/PMU/RTC 初始化，与「不依赖主线」一致。条件编译分支与 `main_lvgl.c` 对齐。

- [x] **Step 3:** 改 `main/CMakeLists.txt`（新增 GLOB、SRCS、INCLUDE_DIRS、注释入口）**与 `main/idf_component.yml`（约束 `esp_lcd_touch ^1.2.1`，见本 Step 末）**。

```cmake
# 在其它 file(GLOB_RECURSE ...) 之后新增：
file(GLOB_RECURSE LVGL_LGFX_CPP_SOURCES ${CMAKE_SOURCE_DIR} "lvgl_lgfx/*.cpp")

# SRCS 里（${LGFX_CPP_SOURCES} 之后、或 ${UI_APP_C_SOURCES} 附近）加入：
    ${LVGL_LGFX_CPP_SOURCES}

# SRCS 的注释入口清单里加入（与其它 main_*.c 注释并列）：
    # "main_lvgl_lgfx.c"

# INCLUDE_DIRS 里加入：
    "lvgl_lgfx"
```

并改 `main/idf_component.yml`：`get_data` 属**底层 `esp_lcd_touch`**（非 `gt911`），它自 **1.2.1** 起提供 `get_data`；在现有 `esp_lcd_touch_gt911: "^1.2.0"` 旁新增一行显式约束（`gt911` 会 transitively 依赖 `esp_lcd_touch`，取交集拉到 1.2.1+）：

```yaml
  esp_lcd_touch: "^1.2.1"
```

（`gt911` 保持 `^1.2.0`，其最新即 1.2.0~2；`get_data` 属底层 `esp_lcd_touch`、其最新为 1.2.1，通常取交集拉到 1.2.1。若该约束与 `gt911` 的依赖解析冲突，**不要盲目删行**——应以 **Task 4 Step 2** 的 `idf.py build` + 实际 `managed_components/espressif__esp_lcd_touch/include/esp_lcd_touch.h` 确认 `get_data` 是否存在：**优先调整依赖版本、设法拉到含 `get_data` 的 `esp_lcd_touch`（放宽/改写约束等）**；仅当确实只能停留在无 `get_data` 的旧版（如 1.2.0）时，才**临时**回退 `get_coordinates`（1.2.0 下它尚未 deprecated）。**若依赖解析持续冲突、无法拉到含 `get_data` 的版本，按 STOP 原则暂停并向用户汇报，不自行大改实现或删依赖。**）

- [x] **Step 4:** 提交。

```bash
git add main/lvgl_lgfx/lvgl_lgfx_hp28008.cpp main/main_lvgl_lgfx.c main/CMakeLists.txt main/idf_component.yml
git commit -m "$(cat <<'EOF'
feat(main): ✨ 新增真机 main_lvgl_lgfx 入口 + hp28008 适配

- lvgl_lgfx_hp28008.cpp：LGFX 只显示(不 setTouch) + esp_lcd_touch_gt911 复用 bus_handle
  + 真实 BM8563 RTC 时钟源 + 触摸按旋转变换 + 自管 LVGL 运行时(esp_timer/task/mutex)
- main_lvgl_lgfx.c：I2C/PMU/RTC 初始化(对齐 main_lvgl.c) 后调 lvgl_lgfx_hp28008_run()
- CMakeLists 注册 lvgl_lgfx/ 源码与 include，注释入口加 main_lvgl_lgfx.c；不动 hp28008.c
- idf_component.yml：显式约束 esp_lcd_touch ^1.2.1，保证 get_data API 可用
EOF
)"
```

---

## Task 4: 真机构建验证（切入口 build，验证后切回）

**Files:** 无需提交的改动（Step 1/4 会临时改 `main/CMakeLists.txt` 切生效入口做 build，验证后还原、不提交）

- [x] **Step 1:** 临时把 `main/CMakeLists.txt` 的 `SRCS` 生效入口从 `main_lvgl_qemu_rgb.c` 改为 `main_lvgl_lgfx.c`（注释掉前者、取消注释后者）。
- [x] **Step 2:** 构建。

Run: `source /opt/esp/idf/export.sh && idf.py build`
Expected: 编译链接通过——无 `app_*` 符号冲突（bridge/适配用 `lvgl_lgfx_` 前缀）、无 LVGL v9 API 错误、无 LovyanGFX/C++ 链接错误。`idf.py size` 供参考。
前提确认：`idf.py menuconfig`（或现有 `sdkconfig`）须为 **`CONFIG_LV_COLOR_DEPTH_16`**（与 `hp28008.c`/LVGL 默认一致）；否则 bridge 的 `rgb565_t`（2 字节/px）假设不成立。

- [x] **Step 3:**（可选）QEMU 冒烟：`timeout 40 idf.py qemu --qemu-extra-args "-no-reboot"`。注意：`app_main` 会先做 AXP202/BM8563/GT911 等真实 I2C 外设初始化，QEMU 无这些外设，故会在某个外设初始化阶段失败/abort——**abort 点不定**：现有 `axp202_init()` 在 built-in 分支失败时 `return false`（≈`ESP_OK`）可能被 `ESP_ERROR_CHECK` 放过而不 abort，故更可能在随后的 `bm8563_init()`（失败 `return ESP_FAIL`）处 abort。无论在哪一步失败/abort 都**属预期**（QEMU 无硬件）。因此 QEMU 不适合验证本真机入口；验证以 Step 2 的 `idf.py build` 通过 + 真机实测为准（见 spec 第 9 节）。
- [x] **Step 4:** 把 `SRCS` 生效入口切回 `main_lvgl_qemu_rgb.c`，并 `git diff -- main/CMakeLists.txt` 确认已还原（无残留 diff）；此切换不提交。

- [ ] **Step 5（真机·需硬件；Cloud 无实体硬件，未执行，留真机人工验证）:** 先对照 `lgfx_test()` 确认**颜色不反相**（红/绿/蓝色块正确）；若整屏颜色反相，调 `lgfx_hp28008.hpp` 的 `cfg.invert`（见其注释：与 `hp28008.h` 的 `EXAMPLE_LCD_INVERT_COLOR=1` 不同源）。再确认**显示旋转方向**——每次点「Rotate screen」后画面应转到正确方向（LVGL 逻辑旋转与 LGFX `setRotation` 的 CW 方向一致）；若反向/错位，用 `offset_rotation` 或 `flush_cb` 里 `setRotation` 的映射（如 `4 - rot`）修正。再做**触摸四角校准**——0° 及 90/180/270 各方向点四角，确认触点与手指位置一致；若某方向镜像/交换，调整 `real_touch_read` 对应 case 的 x/y 公式。以上均为计划内步骤（非可选），截图/录屏留证。

---

## Task 5: PC 工程 `pc_simulator_lgfx/`（CMake + main.cpp + lv_conf.h）

**Files:**
- Create: `pc_simulator_lgfx/lv_conf.h`
- Create: `pc_simulator_lgfx/main.cpp`
- Create: `pc_simulator_lgfx/CMakeLists.txt`
- 复用: `main/ui_app/ui_app.c`、`main/lvgl_lgfx/lvgl_lgfx_bridge.cpp`

- [x] **Step 1:** 创建 `pc_simulator_lgfx/lv_conf.h`：复制 `pc_simulator/lv_conf.h` 全文，**唯一改动**把 `#define LV_USE_SDL 1` 改为 `#define LV_USE_SDL 0`（PC 显示走 LovyanGFX `Panel_sdl`，不用 LVGL 自带 SDL 后端；`LV_COLOR_DEPTH 16`、`LV_FONT_MONTSERRAT_14`、`LV_USE_BUTTON`、`LV_USE_OBSERVER`、`LV_USE_LOG/LV_LOG_PRINTF`、demos/examples=0 等保持不变；`LV_SDL_RENDER_MODE` 行留着无害）。

- [x] **Step 2:** 创建 `pc_simulator_lgfx/main.cpp`（`Panel_sdl::main` + setup/loop，复用同一 bridge）。

```cpp
/*
 * SPDX-FileCopyrightText: 2026 ESP-Pocket2
 *
 * SPDX-License-Identifier: Apache-2.0
 *
 * PC LGFX 模拟器：LovyanGFX Panel_sdl 后端渲染共享 ui_app（经 lvgl_lgfx_bridge），鼠标模拟触摸。
 */
#define LGFX_USE_V1
#include <LovyanGFX.hpp>
#include <lgfx/v1/platforms/sdl/Panel_sdl.hpp>
#include "lvgl.h"
#include "lvgl_lgfx_bridge.hpp"
#include "ui_app.h"
#include <ctime>

/* PC 侧 LGFX 设备：Panel_sdl，尺寸对齐 hp28008 */
class LGFX_PC : public lgfx::LGFX_Device
{
    lgfx::Panel_sdl _panel;
public:
    LGFX_PC(void)
    {
        auto cfg = _panel.config();
        cfg.memory_width  = UI_APP_HOR_RES;
        cfg.memory_height = UI_APP_VER_RES;
        cfg.panel_width   = UI_APP_HOR_RES;
        cfg.panel_height  = UI_APP_VER_RES;
        _panel.config(cfg);
        setPanel(&_panel);
    }
};

static LGFX_PC s_lcd;

/* 触摸源：Panel_sdl 的 getTouch 已随 setRotation 返回逻辑坐标（G7-B，PC 端自动） */
static bool pc_touch_read(int32_t *x, int32_t *y)
{
    int32_t tx = 0, ty = 0;
    if (s_lcd.getTouch(&tx, &ty)) {
        *x = tx;
        *y = ty;
        return true;
    }
    return false;
}

/* 时钟源：本机时间（POSIX localtime_r，线程安全）；失败返回占位串 */
static const char *pc_clock_source(void)
{
    static char buf[32];
    time_t now = time(NULL);
    struct tm tmv;
    struct tm *t = localtime_r(&now, &tmv);
    if (t == NULL || strftime(buf, sizeof(buf), "%Y-%m-%d %H:%M:%S", t) == 0) {
        return "----/--/-- --:--:--";
    }
    return buf;
}

/* LVGL tick 源：用 LovyanGFX 的 millis()（无捕获 lambda 可转函数指针） */
static uint32_t pc_tick_get(void)
{
    return (uint32_t)lgfx::millis();
}

bool setup(void)
{
    /* Panel_sdl::init() 返回 bool（恒真，同真机侧），此检查为防御性/与真机 setup 对称 */
    if (!s_lcd.init()) {
        LV_LOG_ERROR("LGFX Panel_sdl init failed");
        return false;
    }
    s_lcd.setColorDepth(16);

    lv_init();
    lv_tick_set_cb(pc_tick_get);

    static lv_color_t buf1[UI_APP_HOR_RES * LVGL_LGFX_DRAW_BUFF_HEIGHT];
    static lv_color_t buf2[UI_APP_HOR_RES * LVGL_LGFX_DRAW_BUFF_HEIGHT];

    lvgl_lgfx_bridge_cfg_t cfg = {};
    cfg.lcd = &s_lcd;
    cfg.buf1 = buf1;
    cfg.buf2 = buf2;
    cfg.buf_size_bytes = sizeof(buf1);
    cfg.touch_read = pc_touch_read;
    cfg.clock_src = pc_clock_source;
    if (lvgl_lgfx_bridge_create(&cfg) == NULL) {
        LV_LOG_ERROR("lvgl_lgfx_bridge_create failed");
        return false;
    }
    return true;
}

void loop(void)
{
    lv_timer_handler();
    lgfx::delay(5);
}

int user_func(bool *running)
{
    /* setup 失败：结束本用户线程（不再 loop → LVGL 停更）。注意 Panel_sdl::main 用
     * SDL_WaitThread(thread, nullptr) 忽略此返回值、SDL 主循环仍继续（窗口静止但不崩），
     * 进程不因此以非 0 退出；失败仅靠上面的 LV_LOG_ERROR 可见。PC 端 setup 失败极罕见。 */
    if (!setup()) {
        return 1;
    }
    do {
        loop();
    } while (*running);
    return 0;
}

int main(int, char **)
{
    return lgfx::Panel_sdl::main(user_func);
}
```

- [x] **Step 3:** 创建 `pc_simulator_lgfx/CMakeLists.txt`（基于 `pc_simulator/CMakeLists.txt` 骨架 + LovyanGFX SDL 源 + `-DLGFX_SDL`；`project(... C CXX)`）。

```cmake
cmake_minimum_required(VERSION 3.18)
project(pc_simulator_lgfx C CXX)

include(FetchContent)

find_package(SDL2 CONFIG QUIET)
if(NOT SDL2_FOUND)
    find_package(SDL2 REQUIRED)
endif()
find_package(Python3 REQUIRED COMPONENTS Interpreter)

# ---- LVGL v9.5.0（与固件同一 minor；使用本工程 lv_conf.h，关闭 LV_USE_SDL） ----
FetchContent_Declare(
    lvgl
    GIT_REPOSITORY https://github.com/lvgl/lvgl.git
    GIT_TAG 85aa60d18b3d5e5588d7b247abf90198f07c8a63  # v9.5.0
)
set(LV_BUILD_CONF_PATH "${CMAKE_CURRENT_SOURCE_DIR}/lv_conf.h" CACHE FILEPATH "" FORCE)
set(CONFIG_LV_BUILD_DEMOS OFF CACHE BOOL "" FORCE)
set(CONFIG_LV_BUILD_EXAMPLES OFF CACHE BOOL "" FORCE)
if(POLICY CMP0169)
    cmake_policy(SET CMP0169 OLD)
endif()
FetchContent_GetProperties(lvgl)
if(NOT lvgl_POPULATED)
    FetchContent_Populate(lvgl)
    file(REMOVE "${lvgl_SOURCE_DIR}/src/draw/sw/blend/helium/lv_blend_helium.S")
    add_subdirectory("${lvgl_SOURCE_DIR}" "${lvgl_BINARY_DIR}")
endif()

# ---- LovyanGFX（SDL 后端源，参照 examples_for_PC/CMake_SDL） ----
set(LGFX_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/../components/LovyanGFX/src")
file(GLOB LGFX_SOURCES CONFIGURE_DEPENDS
    "${LGFX_ROOT}/lgfx/Fonts/efont/*.c"
    "${LGFX_ROOT}/lgfx/Fonts/IPA/*.c"
    "${LGFX_ROOT}/lgfx/utility/*.c"
    "${LGFX_ROOT}/lgfx/v1/*.cpp"
    "${LGFX_ROOT}/lgfx/v1/misc/*.cpp"
    "${LGFX_ROOT}/lgfx/v1/panel/Panel_Device.cpp"
    "${LGFX_ROOT}/lgfx/v1/panel/Panel_FrameBufferBase.cpp"
    "${LGFX_ROOT}/lgfx/v1/platforms/sdl/*.cpp"
)

# ---- esp_logo.c 构建时生成（venv + LVGLImage.py，同 pc_simulator） ----
set(LVGL_PY_DEPS pypng lz4 Pillow)
set(LVGL_VENV_DIR "${CMAKE_CURRENT_BINARY_DIR}/.lvgl_venv")
set(LVGL_VENV_SENTINEL "${LVGL_VENV_DIR}/.deps_ok")
if(WIN32)
    set(LVGL_PYTHON "${LVGL_VENV_DIR}/Scripts/python.exe")
else()
    set(LVGL_PYTHON "${LVGL_VENV_DIR}/bin/python3")
endif()
if(NOT EXISTS "${LVGL_VENV_DIR}")
    execute_process(COMMAND "${Python3_EXECUTABLE}" -m venv "${LVGL_VENV_DIR}" RESULT_VARIABLE VENV_RESULT)
    if(NOT VENV_RESULT EQUAL 0)
        message(FATAL_ERROR "Python venv 创建失败")
    endif()
endif()
set(LVGL_VENV_DEPS_CACHED "")
if(EXISTS "${LVGL_VENV_SENTINEL}")
    file(READ "${LVGL_VENV_SENTINEL}" LVGL_VENV_DEPS_CACHED)
endif()
if(NOT LVGL_VENV_DEPS_CACHED STREQUAL "${LVGL_PY_DEPS}")
    execute_process(COMMAND "${LVGL_PYTHON}" -m pip install ${LVGL_PY_DEPS} --quiet RESULT_VARIABLE PIP_RESULT)
    if(NOT PIP_RESULT EQUAL 0)
        message(FATAL_ERROR "安装 Python 依赖失败（需访问 PyPI）")
    endif()
    file(WRITE "${LVGL_VENV_SENTINEL}" "${LVGL_PY_DEPS}")
endif()
set(ESP_LOGO_C "${CMAKE_CURRENT_BINARY_DIR}/esp_logo.c")
add_custom_command(
    OUTPUT "${ESP_LOGO_C}"
    COMMAND "${LVGL_PYTHON}" "${lvgl_SOURCE_DIR}/scripts/LVGLImage.py"
        --ofmt C --cf ARGB8888 --compress NONE --name esp_logo
        -o "${CMAKE_CURRENT_BINARY_DIR}" "${CMAKE_CURRENT_SOURCE_DIR}/../main/images/esp_logo.png"
    DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/../main/images/esp_logo.png"
    COMMENT "从 esp_logo.png 生成 esp_logo.c"
    VERBATIM
)

# ---- 可执行：main + 共享 ui_app + 共享 bridge + LovyanGFX SDL 源 + esp_logo ----
add_executable(pc_simulator_lgfx
    "main.cpp"
    "../main/ui_app/ui_app.c"
    "../main/lvgl_lgfx/lvgl_lgfx_bridge.cpp"
    ${LGFX_SOURCES}
    "${ESP_LOGO_C}"
)
target_compile_definitions(pc_simulator_lgfx PRIVATE LGFX_SDL)
target_compile_features(pc_simulator_lgfx PUBLIC cxx_std_17)
target_include_directories(pc_simulator_lgfx PRIVATE
    "../main/ui_app"
    "../main/lvgl_lgfx"
    "${LGFX_ROOT}"
    ${SDL2_INCLUDE_DIRS}
)

if(TARGET SDL2::SDL2)
    set(PC_SIM_SDL2_LINK SDL2::SDL2)
else()
    set(PC_SIM_SDL2_LINK ${SDL2_LIBRARIES})
    target_include_directories(pc_simulator_lgfx PRIVATE ${SDL2_INCLUDE_DIRS})
endif()

# LVGL 关了 LV_USE_SDL，故 SDL2 只链给本可执行（供 LovyanGFX Panel_sdl 用）；Linux 需 pthread
target_link_libraries(pc_simulator_lgfx PRIVATE lvgl ${PC_SIM_SDL2_LINK})
if(CMAKE_SYSTEM_NAME MATCHES "Linux")
    target_link_libraries(pc_simulator_lgfx PRIVATE pthread)
endif()
```

- [x] **Step 4:** 提交。

```bash
git add pc_simulator_lgfx/lv_conf.h pc_simulator_lgfx/main.cpp pc_simulator_lgfx/CMakeLists.txt
git commit -m "$(cat <<'EOF'
feat(pc_simulator_lgfx): ✨ 新增 PC LovyanGFX(SDL) LVGL 模拟工程

- Panel_sdl::main + setup/loop：LGFX_PC(Panel_sdl) 渲染共享 ui_app，经同一 lvgl_lgfx_bridge
- 复用 main/ui_app + main/lvgl_lgfx/lvgl_lgfx_bridge.cpp；触摸=lcd.getTouch，时钟=本机时间
- CMake：LVGL FetchContent(v9.5.0) + LovyanGFX SDL 源(-DLGFX_SDL) + SDL2 + esp_logo 生成
- lv_conf.h 复制自 pc_simulator 并关 LV_USE_SDL（显示走 LGFX 不走 lv_sdl）
EOF
)"
```

---

## Task 6: PC 构建运行验证 + README

**Files:**
- Create: `pc_simulator_lgfx/README.md`

- [x] **Step 1:** 安装依赖、构建并运行验证。

Run（依赖 + 构建；`apt-get install` 若因索引过期失败，先执行 `sudo apt-get update` 再重试，仍失败则 STOP 汇报）:
```bash
sudo apt-get update && sudo apt-get install -y libsdl2-dev cmake build-essential python3 python3-venv
rm -rf pc_simulator_lgfx/build
cmake -S pc_simulator_lgfx -B pc_simulator_lgfx/build
cmake --build pc_simulator_lgfx/build --parallel
```
Expected（构建）: 产出可执行 `pc_simulator_lgfx/build/pc_simulator_lgfx`，无编译/链接错误。

运行验证（图形行为必须看画面、不能只看日志）：`lgfx::Panel_sdl::main()` 是**前台阻塞**循环，会跑到所有窗口关闭才返回（见 `components/LovyanGFX/src/lgfx/v1/platforms/sdl/Panel_sdl.cpp`），故分两种方式：
- 本地（有显示器、人工）：前台运行 `./pc_simulator_lgfx/build/pc_simulator_lgfx`，出现 240×320 窗口显示 `ui_app`（logo/欢迎语/时钟）；鼠标点「Rotate screen」可旋转（0→90→180→270）；关窗即结束。**此路径覆盖旋转交互。**
- Cloud / 无交互（**禁止直接前台跑**，否则会挂起）：**仅验证初始 UI 渲染**——旋转按钮交互不在此路径覆盖，由本地人工验证（可选：用 `xdotool` 模拟点击「Rotate screen」后再截图，但按钮坐标/用法需实测，**不纳入本 plan 必做验证**）。截图用 `ffmpeg`（不在构建依赖内，需单独装）。后台启动 → 等待 → **全屏**截图 → 结束进程：
```bash
# 截图工具 ffmpeg（不在构建依赖内）：装失败先 update 重试；仍不可用则 STOP、勿继续
sudo apt-get install -y ffmpeg || { sudo apt-get update && sudo apt-get install -y ffmpeg; }
command -v ffmpeg >/dev/null || { echo "ffmpeg 不可用，按 STOP 原则暂停并汇报"; exit 1; }
# X display：Cloud 通常 :1；若当前 shell 的 $DISPLAY 为空则回退 :1（可先 ls /tmp/.X11-unix/ 核对，X1→:1）
DISPLAY_ID="${DISPLAY:-:1}"
DISPLAY="$DISPLAY_ID" ./pc_simulator_lgfx/build/pc_simulator_lgfx &
APP_PID=$!
# 兜底：无论后续成功/失败/中断，退出时都回收后台模拟器，避免残留进程
trap 'kill "$APP_PID" 2>/dev/null || true' EXIT
sleep 3
# 全屏抓取（不指定 -video_size，由 ffmpeg 自动取整个 display 尺寸），避免固定区域截不到 SDL 窗口；timeout 防 x11grab 卡住
timeout 10s ffmpeg -f x11grab -i "$DISPLAY_ID" -frames:v 1 -y shot.png
kill "$APP_PID" 2>/dev/null || true
wait "$APP_PID" 2>/dev/null || true
```
核对 `shot.png`（全屏）中是否包含预期 UI（logo/欢迎语/时钟）。

- [x] **Step 2:** 创建 `pc_simulator_lgfx/README.md`（简介/依赖/构建运行/交互/与 pc_simulator 的区别/局限），要点：
  - 本工程是 `pc_simulator` 的 **LovyanGFX 后端变体**：显示/触摸经 `Panel_sdl` + 共享 `lvgl_lgfx_bridge`，验证真机同一份 bridge 代码路径；与 `pc_simulator`（纯 `lv_sdl`）并列、二者都渲染同一 `main/ui_app/`。
  - 依赖：构建/本地运行需 Linux `libsdl2-dev cmake build-essential python3 python3-venv`；Cloud 截图验证另需 `ffmpeg`（`xdotool` 仅可选、不纳入必做验证）；LVGL 由 FetchContent 拉取；LovyanGFX 为仓库 submodule（需 `git submodule update --init --recursive`）。
  - 构建运行命令同 Step 1；首次 configure 需联网（GitHub 拉 LVGL、PyPI 装 pypng/lz4/Pillow）。
  - 交互：鼠标左键=触摸；点「Rotate screen」旋转。
  - 局限：仅渲染 UI，不跑真实固件/驱动；旋转在 PC 上是软件坐标映射（见 spec 8.3，不代表真机性能）。

- [x] **Step 3:** 提交。

```bash
git add pc_simulator_lgfx/README.md
git commit -m "$(cat <<'EOF'
docs(pc_simulator_lgfx): 📝 补 PC LovyanGFX 模拟工程说明

- 简介/依赖/构建运行/交互/与 pc_simulator(纯 lv_sdl) 的区别/局限
EOF
)"
```

---

## Task 7: AGENTS.md 指引更新

**Files:**
- Modify: `AGENTS.md`

- [x] **Step 1:** 在 `AGENTS.md` 的「### PC 模拟（纯 LVGL SDL，免烧录看 UI）」小节之后，追加一段（说明 `pc_simulator_lgfx/` 是 LovyanGFX 后端变体、真机对应入口 `main_lvgl_lgfx.c`）：

```markdown
### PC 模拟（LovyanGFX SDL 后端变体）

`pc_simulator_lgfx/` 是 `pc_simulator/` 的 LovyanGFX 后端变体：显示/触摸经 LovyanGFX `Panel_sdl` + 共享的 `main/lvgl_lgfx/lvgl_lgfx_bridge`（真机 `main_lvgl_lgfx.c` 用同一份 bridge）。构建运行见 `pc_simulator_lgfx/README.md`。真机对应入口是 `main_lvgl_lgfx.c`（LGFX 驱动 HP28008 显示、esp_lcd_touch 触摸、BM8563 RTC）。
```

- [x] **Step 2:** 提交。

```bash
git add AGENTS.md
git commit -m "$(cat <<'EOF'
docs(agents): 📝 补 pc_simulator_lgfx 与 main_lvgl_lgfx 指引
EOF
)"
```

---

## superpowers 文档提交时机（用户要求）

- 本 spec 与 plan 本轮**只写工作区、不 git 提交**（G11=b）。等 review 通过或未来实现时再按需提交。
- 实现开始后，`docs/superpowers/` 的 spec/plan 与代码提交的先后由届时决定；沿用仓库惯例（spec+plan 可合并为一次 `docs` 提交）。

## 验证策略（汇总，见 spec 第 9 节）

- 真机构建：Task 4 切入口 `idf.py build` 通过。
- 真机实测（关键，须截图/拍照）：颜色（对照 `lgfx_test`）、触摸可点、旋转 0/90/180/270 方向与触摸对齐。
- QEMU：不适合验证本真机入口——`app_main` 会在某真实外设初始化阶段 `ESP_ERROR_CHECK` abort（abort 点不定，大概率 `bm8563_init()`；`axp202_init()` built-in 分支失败 `return false`≈`ESP_OK` 可能被放过），进不到显示/触摸，均属预期（QEMU 无硬件）；仅 `idf.py build` 通过 + 真机实测有效（详见 Task 4 Step 3）。
- PC：Task 6 构建运行；Cloud 截图仅核对初始 UI，旋转交互由本地人工覆盖。
- API 核对（实现提示）：本仓库无 `lv_indev_create`/`esp_lcd_touch_get_data` 的直接先例，实现时以 `managed_components` 拉取后的实际头为准核对 LVGL v9（`lv_display_rotation_t` 枚举值=0..3、`lv_indev_set_display`/`lv_indev_set_read_cb`、`lv_tick_set_cb`）与 `esp_lcd_touch`（`esp_lcd_touch_get_data` 签名与 `esp_lcd_touch_point_data_t` 字段）等签名。注：`get_data` 由**底层 `esp_lcd_touch` 1.2.1**（最新版）提供（同时 deprecate `get_coordinates`）；已在 `idf_component.yml` 显式约束 `esp_lcd_touch: "^1.2.1"` 保证其可用（`gt911` 保持 `^1.2.0`，`get_data` 不属 `gt911` 而属其 transitive 依赖 `esp_lcd_touch`）。

## 后续（本 plan 不含）

- 帧率不足时改 flush 为「LVGL 输出 swap565 + pushImageDMA(swap565_t)」（spec 第 10 节）。
- （触摸旋转变换方向的真机校准已升为 Task 4 Step 5 的计划内步骤。）

## 执行结果

> 执行方式：superpowers `subagent-driven-development`——每个 Task 由 implementer 子代理（继承父模型 Claude Opus 4.8）实现，随后两阶段审查（先 spec 合规、后 code quality，两类审查子代理均换用 `gpt-5.5-high-fast` 模型），全部完成后再对整体做一次 final review。分支 `cursor/hp28008-lvgl-lgfx-5194`（base `dev`），最终以 PR9 提交。

### 各 Task 提交与验证

- Task 1 —— commit `792d7cb`：提取 `lgfx_hp28008.hpp` + 改 `lgfx.cpp`（收编全局 count）；`idf.py build` 通过、无 count 重复符号。spec + code quality 审查通过。
- Task 2 —— commit `81177c8`：可移植 bridge `lvgl_lgfx_bridge.{hpp,cpp}`；按 plan 不单独构建。spec + code quality 审查通过。
- Task 3 —— commit `ddefebd` + 修复 commit `29ab6d2`：真机适配 `lvgl_lgfx_hp28008.cpp` + 入口 `main_lvgl_lgfx.c` + CMake/idf_component 注册。`esp_lcd_touch` 依赖解析到 1.2.1，`esp_lcd_touch_get_data`/`esp_lcd_touch_point_data_t` 可用（plan 标注的最大 API 风险点验证通过）。构建通过。spec + code quality 审查通过。
- Task 4 —— 无 commit（切入口验证）：临时切生效入口为 `main_lvgl_lgfx.c`，`idf.py build` 通过（入口 C 文件首次编译+链接，无 `app_*` 符号冲突、无 LVGL v9 API 错误、无 C++ 链接错误），验证后精确切回 `main_lvgl_qemu_rgb.c`（`git diff` 零残留）。`CONFIG_LV_COLOR_DEPTH_16` 前提满足。
- Task 5 —— commit `106fb73`：PC 工程 `pc_simulator_lgfx/`（`lv_conf.h` 仅关 `LV_USE_SDL`、`main.cpp`、`CMakeLists.txt`）。spec + code quality 审查通过。
- Task 6 —— commit `568b47e` + 修复 commit `e51cdc7`：PC `cmake` 配置+构建通过、可执行产出；`ffmpeg` 全屏截图见到完整 UI（ESP logo / 欢迎语 / 本机时钟 / Rotate screen 按钮），初始渲染验证通过（共享 bridge + `Panel_sdl` 链路正常）；补 `README.md`。spec + code quality 审查通过（含 README QEMU 表述修正）。
- Task 7 —— commit `efa16e3`：`AGENTS.md` 追加「PC 模拟（LovyanGFX SDL 后端变体）」小节。spec + code quality 审查通过。

### 执行中遇到的问题（plan 缺陷，均已在本文件权威源修正）

1. **Task 1 Step 1 注释 `*/` 提前闭合块注释**：`EXAMPLE_LCD_*/EXAMPLE_TOUCH_*` 中的 `*/` 会提前结束顶部 `/* ... */` 块注释，导致 `lgfx_hp28008.hpp` 编译失败（`'EXAMPLE_TOUCH_' does not name a type` 等连锁报错）。已修正为 `EXAMPLE_LCD_* / EXAMPLE_TOUCH_*`（`*` 与 `/` 间加空格；纯注释修复、零语义影响）。
2. **Task 3 Step 1 `tp_io_cfg` 宏聚合初始化在 C++ 下 designator 顺序错**：`ESP_LCD_TOUCH_IO_I2C_GT911_CONFIG()` 宏的 designated-initializer 字段序（`.scl_speed_hz` 先于 `.dev_addr`）与 `esp_lcd_panel_io_i2c_config_t` 声明序相反，C++ 要求二者顺序一致故编译失败（C 允许乱序，故 `hp28008.c` 无碍）。已改为 `{}` + 逐字段赋值（与相邻 `tp_cfg` 手法一致，字段值取自该宏、语义等价），并对应 fix commit `29ab6d2`。

### 限制 / 未覆盖

- **Task 4 Step 5（真机颜色/旋转/触摸校准）**：需实体 ESP32-S3 硬件，Cloud 环境无法执行，留真机人工验证（该 Step 保持未勾选）。
- **Task 4 Step 3（QEMU 冒烟，可选）**：按 plan 判断 QEMU 无真实外设、不适合本入口，未执行。
- **PC 旋转交互**：Cloud 截图路径仅覆盖初始 UI 渲染；旋转按钮交互留本地人工验证（plan 既定）。

### Final review 提出的 plan 外改进建议（未擅自实现，待确认是否纳入 plan）

按「严格按 plan、改进点需确认后再纳入」原则，以下 final review 建议**未实现**，留待决定：
1. 为 bridge 增加编译期保护 `#if LV_COLOR_DEPTH != 16` → `#error`，把 spec 的「构建前须确认 `CONFIG_LV_COLOR_DEPTH_16`」前提由人工确认升级为编译期强制（当前以 Task 4 Step 2 人工前提确认覆盖，构建已验证 `=16`）。
2. 扩充 `.gitignore` 忽略 `sdkconfig` / `managed_components/` / `dependencies.lock` / `main/images/esp_logo.c`（当前各提交均精确 `git add`、未误纳入，此为防未来 `git add -A` 误加）。
